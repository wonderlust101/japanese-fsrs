# AI And Japanese Content Status

Refreshed by code inspection on 2026-05-18 (rename reconciliation pass). AI surface unchanged since 2026-05-14 Stage 7 refresh; this pass updates symbol names to match the 2026-05-18 end-to-end leech → weak spot rename (kanban Done). See [../IMPLEMENTATION_STATUS.md](../IMPLEMENTATION_STATUS.md) for the status legend and summary.

| Capability | Status | Evidence |
|---|---|---|
| AI card generation from a word | Implemented | `apps/api/src/routes/ai.ts`, `apps/api/src/services/ai.service.ts`, `packages/shared-types/src/schemas/ai.schema.ts` |
| AI example sentence generation | Implemented | `apps/api/src/routes/ai.ts`, `apps/api/src/services/ai.service.ts` |
| AI mnemonic generation | Implemented | `apps/api/src/routes/ai.ts`, `apps/api/src/services/ai.service.ts` |
| AI weak-spot diagnosis + prescription | Implemented | `apps/api/src/routes/weak-spots.ts` (`POST /:id/diagnose`), `apps/api/src/services/weak-spot.service.ts` (`diagnoseWeakSpot`), `apps/api/src/services/ai.service.ts` (`generateWeakSpotDiagnosis`), `packages/shared-types/src/schemas/ai.schema.ts` (`GeneratedWeakSpotDiagnosisSchema`). Function/file/schema names renamed in the 2026-05-18 end-to-end rename (kanban Done). Free MVP feature — gated by auth + AI rate limits only. Replay-on-existing semantics keep OpenAI cost bounded. See [BACKEND.md](BACKEND.md) for the full evidence trail. |
| OpenAI response caching | Implemented | `apps/api/src/services/ai.service.ts`, `apps/api/src/db/redis.ts` |
| Prompt sanitization and structured output validation | Implemented | `apps/api/src/services/ai.service.ts`, `packages/shared-types/src/sanitize.ts`, `packages/shared-types/src/schemas/ai.schema.ts` |
| Tomo daily note insight/idiom API | Implemented | **Updated 2026-05-17 (Backend Completion Plan Stage 6 shipped):** `GET /api/v1/tomo/note` returns `{ body, kind: 'insight' \| 'idiom', dateKey }`. AI path: `apps/api/src/services/ai.service.ts::generateTomoNote` (prompt voice rules baked in; consumes learner profile + yesterday's `count`/`retention` from `get_heatmap_data`), Redis-cached per `(userId, dateKey, TOMO_NOTE_PROMPT_VERSION)`. 36h TTL; cost ceiling is one OpenAI call per learner per day. Idiom fallback: `apps/api/src/data/idioms.json` (curated by JLPT level, 3–4 entries each, deterministically picked by SHA-256 of `(userId, dateKey)`) fires whenever the AI path fails — the route never returns a 5xx for content-availability reasons. Orchestration in `apps/api/src/services/tomo-note.service.ts`; controller/route at `apps/api/src/controllers/tomo.controller.ts` + `apps/api/src/routes/tomo.ts` mounted at `/api/v1/tomo`. Auth + `defaultUserRateLimitMiddleware` + `aiRateLimitMiddleware` + `aiDailyQuotaMiddleware`. Frontend wired 2026-05-18, distilled into the hero 2026-05-20: `getTomoNoteAction` (`apps/web/lib/actions/tomo.actions.ts`) + `useTomoNoteQuery` (`apps/web/lib/api/tomo.ts`) feed `HeroPreSessionNote`, the elevated note slot inside `<DashboardHero>` (`apps/web/app/(app)/today/_components/today-hero.tsx`). The standalone `TodayPreSessionNote` block and its passage chrome (ISO date, Japanese weekday, Kotowaza pill, vermillion rule, diamond seal, four chained reveal animations) were retired so the note sits inside the today's-plan section the learner is already reading before pressing Start. Priority unchanged: unresolved weak spots win the slot, the Tomo note fires only when the weak-spots query settles with zero items, so the AI daily quota is only spent on calm days. The note renders as one calm prose line on `text-sumi-ink` at body-plus size, matching PRODUCT.md principle #6 ("Tomo is presence, not personality") — the prior tiny inline kitsune glyph on `text-faded-sumi` is gone with the rest of the passage chrome. Coverage: unit tests for the pure date / idiom-pick helpers; cache-key version-bypass test in `ai.service.test.ts`; integration test for wire shape + same-day cache stability + 401 unauth. |
| Embeddings for similar-card search | Implemented | `apps/api/src/services/card.service.ts`, `apps/api/scripts/backfill-premade-embeddings.ts` |
| Pitch accent, frequency rank, collocations, kanji breakdown fields | Implemented | Shared field schemas allow these fields in `packages/shared-types/src/schemas/field-shapes.schema.ts` (`pitchAccent`, `frequencyRank`, `collocations`, `kanjiBreakdown` on `VocabularyFieldsDataSchema`); older dedicated DB columns were removed in migrations and no specialized UI workflow was verified. **Updated 2026-05-17 (Backend Completion Plan Stages 1 + 2 shipped):** the additive Lapis-style fields the review UI is waiting on — `picture`, `expressionAudio`, `sentenceAudio`, `pitchPosition`, `nuance` — exist on `WordFieldsSchema` / `ExampleSentenceSchema` as `.nullable().optional()` keys (`pitchPosition` constrained to integer ≥ 0). Stage 2 wires the AI generator: `GeneratedCardDataSchema` admits all five keys (so structured-output validation does not strip them), and the `generateCard` prompt now requests `pitchPosition` + `nuance` while explicitly telling the model NOT to invent URLs for `picture` / `expressionAudio` / `sentenceAudio` (those require hosted assets the system can't yet produce). `CARD_PROMPT_VERSION = 'v2'` is baked into the Redis cache key so pre-Stage-2 cache entries are bypassed forever — zero-downtime cache invalidation, mirroring the `DIAGNOSIS_PROMPT_VERSION` pattern. Coverage: six new tests in `ai.service.test.ts` plus the existing 11-case suite in `field-shapes.schema.test.ts`. No migration was required: the `cards_fields_data_shape` CHECK enforces only the required keys (`word`, `reading`, `meaning`) for vocabulary/grammar and does not whitelist others, so additive shape changes pass through without DB work. **Updated 2026-05-20 (Stage 3 shipped):** the prompt grew to also request `collocations` (string[]; 2–6 natural co-occurring phrases) and `homophones` (string[]; same-reading words with brief glosses), and widened the `kanjiBreakdown` shape from `{kanji, meaning}` to `{kanji, meaning, radical?, reading?}` so the structured-output validator no longer strips `radical` / `reading` that the v4 review surface renders (per-character `radical` + `reading` opt-out admitted so the model can omit when unsure rather than fabricate). `GeneratedCardDataSchema` admits all three. `CARD_PROMPT_VERSION` bumped `v2 → v3`; pre-Stage-3 cache entries are bypassed via the new `card:v3:…` key shape. Bug fix in the same change: the review surface helper (`apps/web/components/review/session/card-fields.ts`) and the edit-form 412-reseed (`apps/web/app/(app)/add/review/_components/generated-review-client.tsx`) were both reading `example.audio` from example sentences when the schema field is `sentenceAudio` — silently `null` for every audio-carrying card and a silent data-loss path through the conflict reseed (every 412 wiped the persisted URL). Both call sites now read typed via `getWordFields` / `getVocabularyFields` / new `getSentenceFields` helper (`packages/shared-types/src/field-shapes.ts`); `Record<string, unknown>` casts dropped. Coverage: 4 new tests in `ai.service.test.ts` (collocations + homophones admission, full `kanjiBreakdown` round-trip, per-character opt-out, empty-arrays-are-not-undefined); existing v2 cache-key tests retargeted to v3. **Updated 2026-05-23 (audio + related-words removal):** `collocations`, `homophones`, and all audio fields (`WordFields.expressionAudio`, `ExampleSentence.sentenceAudio`, `SentenceFieldsData.audio`) were removed from the card model end-to-end — shared-types schemas, `GeneratedCardDataSchema` / `GeneratedSentenceCardSchema`, the `generateCard` and `generateSentenceCard` prompts, the review display + `/add/review` editor, and the cards-browser `audio` filter (enum + `list_cards_cross_deck` / `count_cards_cross_deck` branches + UI + `cards_has_audio_idx`). `CARD_PROMPT_VERSION` bumped `v3 → v4`; `SENTENCE_CARD_PROMPT_VERSION` bumped `v1 → v2`. Migration `20260630000005_remove_audio_collocations_homophones.sql` scrubs the removed keys from existing `fields_data` (top-level + nested `sentenceAudio`) and recreates both cross-deck RPCs without the audio arm. `picture` remains schema-admitted-but-prompt-omitted. Tests retargeted to the removed-key-is-dropped contract; cache-key tests moved to v4. **Updated 2026-05-23 (sentence-generation UX):** `POST /api/v1/ai/generate-sentences` now accepts **either** `cardId` (existing: deck-active guard + server-side word lookup) **or** a raw `word` (new: the /add/review pre-save path, no card yet), enforced by a refined `generateSentencesInputSchema` (exactly one of the two). It also accepts an optional `avoid: string[]` of existing sentences; the prompt is told to avoid them and a hash of `avoid` joins the Redis cache key, so repeated "generate more" / per-sentence regenerate calls return fresh, distinct output instead of replaying a cached batch (empty `avoid` keeps the legacy key shape). This fixes the prior pre-save path, which abused the cached full-card generator and returned the same sentence on every click. Frontend: `/add/review` example-sentence editor is now a single-expand accordion (collapsible summary rows) with batch "Generate examples" (count 3, deduped on append), per-row regenerate, and skeleton placeholders. Coverage: avoid-namespaced cache-key test + 4 word-or-cardId schema-validation tests in `ai.service.test.ts`. **Updated 2026-05-23 (POS + pitch-accent enums):** `partOfSpeech` and `pitchAccent` are now closed vocabularies. New `partOfSpeechEnum` in `card.schema.ts` (`Noun`, `る verb`, `う verb`, `Irregular verb`, `い adjective`, `な adjective`, `Adverb`, `Particle`, `Conjunction`, `Expression`, `Counter` — verb/adjective classes use the hiragana convention); `pitchAccent` reuses the existing `pitchPatternEnum` (`heiban`/`atamadaka`/`nakadaka`/`odaka`). `GeneratedCardDataSchema` validates both as `enum.optional().catch(undefined)` so an off-enum model response is dropped rather than failing the whole card. The `generateCard` prompt lists the literal allowed values for each. `/add/review` renders both as `TomoSelect` dropdowns sourced from the same enums, so the manual form, AI prompt, and structured-output validation share one source. `CARD_PROMPT_VERSION` bumped `v4 → v5`. `fields_data.partOfSpeech` / `pitchAccent` remain free strings at the DB layer (no migration); the enum constrains only the selector + AI output. Cache-key tests retargeted to v5. |
| Sentence-layout card workflow | Implemented | **Updated 2026-05-17 (Backend Completion Plan Stages 12 + 13 shipped):** the full sentence-layout pipeline is live end-to-end. Stage 12 tightened `SentenceFieldsDataSchema` to a concrete `{ ja, en, furigana, breakdown?, audio?, nuance? }` shape and added the matching DB CHECK arm via `supabase/migrations/20260612000000_sentence_layout_check.sql`. Stage 13 adds the AI generator branch: new `GeneratedSentenceCardSchema` in `packages/shared-types/src/schemas/ai.schema.ts`; new `generateSentenceCard` in `apps/api/src/services/ai.service.ts` with `SENTENCE_CARD_PROMPT_VERSION = 'v1'` baked into a separate Redis cache namespace (`sentence-card:v1:…`) so vocabulary and sentence prompts can version-bump independently. Controller `apps/api/src/controllers/cards.controller.ts` dispatches on `input.layoutType === 'sentence'` inside the `mode='ai'` branch. Both manual (`mode='manual'`) and AI (`mode='ai'`) creation paths now produce sentence-layout cards that round-trip through POST + GET and pass the Stage 12 CHECK. **Remaining (frontend follow-ups, out of backend scope):** manual sentence-card creation form (currently the AI path is the primary surface; the manual path is available but no dedicated UI) and sentence review render rules (the back of a sentence card needs to render the breakdown / nuance differently from the vocabulary card). |
| Morphological parsing tokens | Missing | Migration `20260502000004_align_card_schema.sql` added `cards.tokens` and `cards.parsed_at`, but migration `20260518000000_drift_and_dead_column_cleanup.sql` removed them. No current parser route, service, shared schema, or canonical database fields were found. |
