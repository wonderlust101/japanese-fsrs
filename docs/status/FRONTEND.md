# Frontend Status

Refreshed by code inspection on 2026-05-19 (deck-list copy + archive wiring; premade attribution chip; row-click bug fix). See [../IMPLEMENTATION_STATUS.md](../IMPLEMENTATION_STATUS.md) for the status legend and summary.

This refresh logs the 2026-05-19 deck-management slice: copy / archive / unarchive hooks landed in `apps/web/lib/api/decks.ts`, `useArchiveSet` was rewritten as a backend-backed wrapper around `useDecks(50, 'all')` with a one-shot localStorage→server migration, the `deck-list.tsx` "Copy is coming soon" toast stubs are now real mutations, and the `/decks` rows surface a "From: <premade name>" attribution chip resolved client-side via `usePremadeDecks`. A nested-`<Link>` bug in `DeckCard` (action button routing to the row's deck-detail link instead of its own href) was fixed by moving the action cluster out of the row's `<Link>` and into a DOM-sibling slot. Prior 2026-05-17 refresh: corrected stale routes from the IA rename (`/dashboard` → `/today`, `/analytics` → `/insights`) and the Leech → Weak spot rename (route `/insights/leeches/*` → `/insights/weak-spots/*`; internal identifiers also renamed in the 2026-05-18 sweep to match `/api/v1/weak-spots`).

| Capability | Status | Evidence |
|---|---|---|
| App shell, protected routes, today, decks, review, insights, settings | Implemented | `apps/web/app/(app)/layout.tsx`, `apps/web/app/(app)/today/page.tsx`, `apps/web/app/(app)/decks/page.tsx`, `apps/web/app/(app)/review/setup/page.tsx`, `apps/web/app/(app)/insights/page.tsx`, `apps/web/app/(app)/settings/page.tsx` |
| IA route rename (phase 1) — page titles + outgoing links only | Partial | **Code-verified 2026-05-17:** only `apps/web/app/(app)/cards/[cardId]/repair/page.tsx` remains as a `StubPage`. `/cards/page.tsx` is now a fully live cards browser (see "Cross-deck cards browser end-to-end" row below); `/decks/[id]/preview` shipped 2026-05-18 as a read-only deck viewer. Real implementations also live at `/add`, `/add/review`, and across all Insights tabs. The `/insights/{mistakes,progress,forecast,statistics}` stubs were retired 2026-05-15 in favor of the Insights Overview redesign — see the new row below. |
| Cross-deck cards browser end-to-end (`/cards`) | Implemented | **Shipped 2026-05-18.** `apps/web/app/(app)/cards/_components/cards-browser-view.tsx` was rewritten to drive off live data: `useCardsCrossDeckQuery` (powered by the new `GET /api/v1/cards/cross-deck`) replaces the dev-fixture `devState.rows` source; `useCardQualityIssuesQuery` (Stage-8 `/insights/card-quality`) populates `CardsQualityBars`; row actions are real mutations (delete → `useDeleteCardMutation` + confirm dialog, move → `useMoveCardMutation` + `MoveCardDialog`, add-copy → `useCopyCardMutation` + `MoveCardDialog` in `variant="add"`, edit → `router.push('/cards/:id')`); `CardsBulkBar` mounts on selection and wires bulk-delete (confirm dialog) + bulk-move (`MoveCardDialog`) + bulk-suspend. Selection state is a `Set<string>` lifted into the browser view; `CardsResultsTable` grew `selectedIds` / `onToggleSelection` / `onToggleAllVisible` props that render a leading-edge checkbox column when active. Saved views (`saved-views-storage.ts`) ship as six curated presets (`recent`, `low-mastery`, `suspended`, `missing-mnemonic`, `missing-example`, `missing-picture`) with the active id persisted to `localStorage`. Backend RPC contracts live in `supabase/migrations/20260617000000_*.sql` (list + count cross-deck) and `20260617000001_*.sql` (move/copy/suspend/unsuspend + five bulk RPCs). The `?missing=<field>` query param from the quality bars hydrates the filter on first mount. Pitch / has-image / has-audio dimensions shipped 2026-05-19 as a real `more-filters-popover.tsx` (portal-rendered, staged Apply/Reset, outside-click + Escape close, focus moved into popover on open, `prefers-reduced-motion` respected). Three independent dimensions — pitch presence + optional pattern (heiban/atamadaka/nakadaka/odaka), image presence, audio presence. URL deep-link via `?present=` and `?pitchPattern=` reads once on mount (same pattern as `?missing=`). Active-filter count badge on the trigger chip. Saved-view recipes carry optional `presentField` / `pitchPattern`. Backend migration `20260624000000_cards_browser_presence_filters.sql`. Known v1 constraint: backend's `.refine` blocks `missingField + presentField` simultaneously, so when a user expresses both directions in the popover only the positive one is sent (`presenceToActionParams` precedence) — tracked as a backend follow-up. |
| Generated Card Review (IA `07_generated_card_review.md`) | Implemented | `apps/web/app/(app)/add/review/page.tsx` mounts `apps/web/app/(app)/add/review/_components/generated-review-client.tsx`. Two entry paths (AI generation vs manual handoff) keyed off `CaptureDraft.mode`; flippable card preview reuses `CardFront` / `CardBack` from the live review session; "Build the back of your card" field stack with inline regenerate links on AI-authored fields; `Advanced` disclosure for pitch/POS/tags; card-type chooser anchored above Save with live count copy; inline success state with Add another / Return to Today / Open cards. Open follow-ups: real mnemonic regeneration (needs a saved card id), image generation, sentence/meaning conflict callout at field level rather than as a single Save-block blocker line. |
| Insights Overview redesign (IA `13_insights_overview.md`) | Implemented | Insights surface now renders a calm teacher-report Overview at `apps/web/app/(app)/insights/page.tsx` via `InsightsOverview`. Siblings under INSIGHTS are Overview / Progress / Forecast / Statistics — Mistakes was retired 2026-05-17 (card-quality moved to `/cards`; pattern, problem-cards, and confusable-pairs surfaces removed entirely, the latter two with their backend endpoints in 2026-05-18). Headline insight selection lives in `_components/headline-insight.ts` (date-seeded, priority-ranked). Follow-ups: per-deck forecast filter, retention timeline chart, broader headline-insight template pool. |
| Login/signup UI with verify route | Implemented | `apps/web/app/(auth)/login/page.tsx`, `apps/web/app/(auth)/signup/page.tsx`, `apps/web/app/(auth)/signup/verify/page.tsx` |
| Multi-step onboarding UI | Partial | Level, goal, interests, schedule, and decks pages exist under `apps/web/app/onboarding`. The decks step now pulls live recommendations from `usePremadeDecks` filtered by the JLPT level chosen on `/onboarding/level` (`beginner` and `null` fall through to N5; N1 also includes `beyond_jlpt`); level-agnostic decks (Joyo Kanji etc., `jlpt_level NULL`) sit beneath the level-matching rows. Loading skeletons + an empty-state notice for "no decks at this level yet" replace the prior hardcoded `RECOMMENDED_DECKS` array. **Selections now ship to the backend (2026-05-18):** "Add and begin" fires `updateProfileAction` + one `copyPremadeDeckAction(deckId)` per selected deck in parallel via `Promise.allSettled`. Profile-mutation failure surfaces inline and blocks navigation; partial deck-copy failures still navigate but show a summary `Toast` ("Added X of Y decks. You can add the rest from the catalogue."). Continue button gates on both phases via the existing `continueLoading` prop. Skip path is unchanged — no profile update, no copies. **Remaining:** layer interest-based scoring on top of the level-only ranking. |
| Premade deck browsing UI | Implemented | `/decks/premade` is now a real catalogue (`apps/web/app/(app)/decks/premade/page.tsx` + `_components/premade-catalogue.tsx`) wired to `usePremadeDecks` and `useCopyPremadeDeck`. Reuses `PageHeader`, `SectionCard`, `Pill`/`JlptPill`/`ContentTypePill`, `Button`, `QuietLink`, `Skeleton`, and the standard `Toast` pattern. Client-side filters by JLPT level and content type; loading skeletons, empty/error states, and a success toast on copy. Entry points: a "Browse premade" link in the `/decks` TopBar (visible from `sm`), the existing `EmptyState`, and the `PremadeStarterRail` (active count ≤ 2). **Already-copied indicator (2026-05-18):** each catalogue row pulls `useDecks` and looks for any user deck whose `sourcePremadeId` matches the catalogue entry; matches render an `"In your library"` `StatusPill` next to the deck name and swap the primary action to a "View deck" `Button` linking to `/decks/[id]/preview`. "Add another copy" stays available as a `QuietLink` so the copy model's intentional duplicates remain reachable. **2026-05-19:** the deck-row attribution chip on `/decks` and the delete-deck copy unification shipped (see "Deck list management" row). |
| Deck list management (`/decks`) — copy, archive, attribution | Implemented | **Shipped 2026-05-19.** Three slices in one PR. **Duplication:** `useCopyDeck` (`apps/web/lib/api/decks.ts`) wraps `POST /api/v1/decks/:id/copy`; `deck-list.tsx`'s `handleCopy` / `handleBulkCopy` (previously "Copy is coming soon" toast stubs) now fire the real mutation. Empty body → server picks the default `<name> (Copy [N])`. Bulk copy uses the same `Promise.allSettled` partial-success pattern as bulk delete; failure tallies surface in the result toast. **Archive:** `useArchiveDeck` / `useUnarchiveDeck` hooks + `archiveDeckAction` / `unarchiveDeckAction` server actions land; `useDecks(limit, view)` now plumbs `view: 'active' \| 'archived' \| 'all'` through to the API. `useArchiveSet` in `use-deck-prefs.ts` no longer reads/writes `localStorage` — the canonical set derives from `useDecks(50, 'all')` and the mutate verbs forward to the API hooks. TanStack Query dedupes the underlying fetch with the catalogue. A one-shot localStorage→server migration runs on first authenticated mount: any deck ids still in the legacy `tomo.decks.archived.v1` LS key are POSTed to `/archive` (idempotent on the server), then the LS key is cleared and a `…v1.migrated` sentinel set so the migration never replays. **Attribution chip:** `DeckCard` accepts `sourcePremadeName: string \| null`; `deck-list.tsx` resolves the name client-side via `usePremadeDecks` → `Map<premadeId, name>` (shared cache with `/decks/premade` so no extra fetch on the typical path). The chip renders as a quiet "· From: <name>" after the cards count. **Delete copy unified** between `DeleteDeckDialog` and `BulkDeleteDecksDialog` — both bodies now read parallel ("Its N cards…" / "Their cards…"). **Row-click bug fix:** action cluster (primary action + kebab) was nested inside the row's `<Link>`, producing invalid `<a>` inside `<a>` HTML and routing every action-button click to the row's deck-detail link. Moved the cluster to a DOM sibling slot in the row's flex container; visual layout unchanged. |
| Read-only deck preview at `/decks/[id]/preview` | Implemented | Shipped 2026-05-18. `apps/web/app/(app)/decks/[id]/preview/page.tsx` (server component, resolves the deck via `getDeckAction`, redirects to `/decks` if missing) + `_components/deck-preview-view.tsx` (client). Reuses the deck-detail surface chrome — `TopBar`, `PageHeader`, search-only toolbar, `CardsResultsTable`, `CardListPagination` — with personal-stats + editing affordances suppressed. The deck-detail TopBar kebab, "Study deck" CTA, FSRS status filter pills, status pill column, due column, lapse-tier row tinting, health badges, and the per-row kebab are all stripped via a new `readOnly` prop on `CardsResultsTable`; the snapshot aside and `DeckOptionsPanel` aren't rendered. A "read-only preview" banner across the top names the surface and offers `Open deck` (routes back to `/decks/[id]`). Reached from the premade catalogue's "View deck" indicator when a learner has already copied a premade entry. |
| Review session UI | Implemented | `apps/web/app/(app)/review/session/page.tsx`, `apps/web/components/review/ReviewCard.tsx`, `apps/web/components/review/RatingButtons.tsx`, `apps/web/stores/useReviewSessionStore.ts` |
| Offline review queue and replay | Implemented | `apps/web/lib/offline-queue.ts`, `apps/web/lib/api/reviews.ts`, `apps/web/app/(app)/review/_components/offline-queue-banner.tsx` |
| Review summary page | Implemented | `apps/web/app/(app)/review/summary/page.tsx`, `apps/api/src/routes/reviews.ts` |
| Insights dashboard UI | Implemented | `apps/web/app/(app)/insights/_components/*` (renamed from `/analytics` during the IA migration), `apps/web/lib/api/analytics.ts`. Forecast / Statistics / Progress / Weak spots all ship as real implementations; Mistakes was deleted 2026-05-17 with its content redistributed. The legacy `StreakCard` was removed end-to-end in Stage 8 along with `ApiAnalyticsDashboardSchema.streak`. **Statistics live-data wire (2026-05-18):** `/insights/statistics` no longer rendered the page only when a dev fixture was selected. The five sections (Activity, Retention, Cards, Scheduling, FSRS) are now driven by an adapter (`_components/adapt-live.ts`) that maps `ApiAnalyticsDashboard` (heatmap) + `ApiMaturitySnapshot[]` (the Stage 9 `insights/maturity-history` endpoint, newly wired in `lib/actions/insights.actions.ts` + `lib/api/insights.ts`) + `ApiDeck[]` (decks) + `ApiForecastDay[]` (review forecast) + `profile.retentionTarget` onto the existing `StatisticsData` shape. The dev panel's `fixtureData` overrides live inputs when present so designers can still preview every state. Sections whose data isn't on the wire yet — answer-button distribution, FSRS stability/difficulty distributions, FSRS optimization status, interval distribution — pass zeros / empty arrays and the existing chart components render their "no data yet" notice. Live empty state (no reviews recorded) routes through the same calm Statistics-empty surface the page already had; the loading state fires when the dashboard query is in-flight. **Progress Mature section live wire (2026-05-18):** `/insights/progress` no longer left the Mature card stubbed with "Per-day pipeline snapshots arrive in the next backend pass." It now consumes `useMaturityHistory('365')` and maps each `ApiMaturitySnapshot` onto a `MaturePoint` (collapsing learning + relearning, deriving `young = max(0, reviewCount - matureCount)`). Milestone dots at 100 / 250 / 500 / 1,000 / 2,500 / 5,000 mature cards are computed by walking the sorted series and dating each threshold to the first crossing. `summary.matureCount` now comes from the latest snapshot instead of being hardcoded to 0. The 14-day `hasMature` guard already in place keeps the calm "a few more weeks of practice will fill this chart" notice for low-history learners; the prior "endpoint doesn't yet return" disclaimer is gone. **Progress + Statistics final wire signals (2026-05-18):** `desiredRetention` now reads `profile.retentionTarget` on Progress (Statistics already did); `cardsAddedThisMonth` flows through the analytics dashboard's new tz-aware `cardsAddedThisMonth` field instead of a hardcoded zero. The four previously empty Statistics histograms (answer-button rating distribution, interval distribution, FSRS stability, FSRS difficulty) consume `GET /api/v1/insights/distributions` — bundled round-trip via the new `useInsightsDistributions` hook. The activity-strip "total time" figure dropped its `SECONDS_PER_REVIEW = 18` heuristic and now sums per-day `totalSeconds` from the extended `get_heatmap_data` RPC. FSRS `optimizationStatus` is the lone deferral — there's no FSRS-weight refit process today, so the chart still reads `'never-run'` by design (see KANBAN_BOARD.md). |
| Today live-data modules and deferred modules | Partial | `apps/web/app/(app)/today/_components/today-client.tsx` (renamed from `/dashboard` during the IA migration) drives the hero from due cards, decks from `GET /api/v1/decks`, forecast from split review-forecast data, and recent activity from heatmap data. **Practice-signal card live-wire (2026-05-18):** the IA's "concise pre-session note (weak-point reminder)" slot now ships as `_components/today-weak-spots-card.tsx`, consuming `useWeakSpotsQuery({ status: 'unresolved', limit: 3, sort: 'mostRecent' })` (the same cache the sidebar count badge populates). The card sits between `<DashboardHero>` and `<WeekRhythmStrip>`, renders count + up to two compact preview rows (word/reading via `FuriganaText`, deck name, lapse count, JLPT pill — no diagnosis line, no per-row CTAs), and carries two `QuietLink` CTAs ("Drill these weak spots →" → `/insights/weak-spots/drill/setup`, "See all →" → `/insights/weak-spots`, the second one only when more than the preview limit are outstanding). Calm-first behaviour: silent during `isLoading`/`isError` and DOM-absent when the learner has no unresolved weak spots (mirrors the sidebar badge's silence pattern). Dev-only preview controls still use sample data for hero + week-rhythm. |
| Weak spots surface (Phase 1 — list + detail + diagnosis) | Implemented | `apps/web/app/(app)/insights/weak-spots/page.tsx` + `_components/*` ship the dedicated `/insights/weak-spots` route (renamed from `/insights/weak-spots` on 2026-05-17 — backend `/api/v1/weak-spots` and internal `weak spot*` symbols unchanged) with status / deck / JLPT / modality / diagnosis filters, sort options, per-row Drill / View / Resolve / Reopen actions, a `Dialog`-based detail surface with FSRS state grid, and a `Diagnose this weak spot` panel calling `POST /api/v1/weak-spots/:id/diagnose` (Idempotency-Key minted per request). API layer is `apps/web/lib/actions/weak-spots.actions.ts` + `apps/web/lib/api/weak-spots.ts` with cache keys including the full filter object. Sidebar discoverability shipped via `nav-config.ts` (Weak spots in STUDY) with a `WeakSpotCountNavItem` wrapper rendering a `50+`-capped mono count chip when there's work to do — wired into both `sidebar.tsx` and `mobile-drawer.tsx` via top-level `hasWeakSpotCount` dispatch on 2026-05-19 (prior to that the in-file dispatch only fired for child rows, so the chip never reached either chrome surface). The diagnosis panel carries a closed-by-default "About this read" `MoreDisclosure` (2026-05-19) explaining what Tomo's read is generated from. |
| Weak spots drill flow (Phase 2 — setup + session + summary) | Implemented | `apps/web/app/(app)/insights/weak-spots/drill/*` ships `/setup`, `/[sessionId]`, and `/[sessionId]/summary` routes. Bespoke `DrillRatingBar` carries the 3-channel `missed / hesitated / remembered` vocabulary per the design brief (not FSRS again/hard/good/easy). `useWeakSpotDrillSessionStore` (Zustand) holds queue + attempts with idempotent `eventId` per attempt; five new server actions + hooks wrap the `/api/v1/weak-spots/drill-sessions/*` endpoint family. Layout is a fixed-overlay zen pattern (no sidebar during a drill). **Phase 3 (2026-05-19):** "Count this as a real review" override shipped — inline 4-channel confirm panel inside `drill-session-client.tsx` activates while the answer is showing (`R` opens, `Esc` cancels, `1-4` map to Again/Hard/Good/Easy while the panel owns the keymap), submits via the canonical `useSubmitReview` mutation, advances the local queue with `countedAsReview: true`, skips the drill-attempt network write, and invalidates `queryKeys.weakSpots.all()` so the unresolved-count badge refreshes. |
| Review personal-best persistence | Partial | `apps/web/app/(app)/review/summary/page.tsx` uses a `localStorage` placeholder for personal-best comparison; no durable user-scoped persistence was found. |
| Public brand landing page | Partial | `apps/web/app/page.tsx` exists, but it currently redirects to `/onboarding`; no completed public brand surface was found. |
| Public SEO and installability surfaces | Partial | SEO baseline shipped: `apps/web/app/layout.tsx` sets `metadataBase` and root `robots: { index: true, follow: true }`, `apps/web/app/(auth)/layout.tsx` and `apps/web/app/onboarding/layout.tsx` mark private trees `noindex`, and `apps/web/app/robots.ts` plus a minimal `apps/web/app/sitemap.ts` exist. Still missing: `app/manifest.ts`, maskable/monochrome icon variants, dynamic deck Open Graph images, Organization/SoftwareApplication JSON-LD, and expanded public sitemap routes (features, pricing, privacy, help). |
| Full frontend CSP nonce policy | Partial | `apps/web/next.config.ts` sets security headers and `frame-ancestors`, but comments state a fuller script/style CSP is deferred until Next.js nonce handling is designed. |
| Product design-system migration to current Tomo brand | Partial | Brand assets, today-page masthead imagery, dev-only component showcase, and custom icon components exist in `apps/web/public/brand`, `apps/web/public/assets/dashboard`, `apps/web/app/dev/components`, and `apps/web/components/icons`. **Code-verified 2026-05-17:** `lucide-react` is import-free across `apps/web/{app,components,lib}` — only a single *comment* reference survives in `apps/web/components/icons/arrow-glyph.tsx:5` documenting the "never lucide" rule; the kanban moved this sub-item to Done. The remaining partial portion is the design-drift item on the board: restyling the today-page deck list, refining the My Decks page, replacing the masthead background illustration, and updating the insights navigation icon. |
| Legal pages | Missing | No privacy or terms pages were found under `apps/web/app`; the Kanban board tracks this as active work. |
| Custom error/not-found/loading pages | Partial | **Code-verified 2026-05-17:** `apps/web/app/error.tsx`, `apps/web/app/global-error.tsx`, `apps/web/app/not-found.tsx`, `apps/web/app/(app)/error.tsx`, and `apps/web/app/(app)/not-found.tsx` all exist. **Missing:** any `loading.tsx` file — `find apps/web/app -name 'loading.tsx'` returns nothing. Streaming-aware skeletons for `/today`, `/review/setup`, and `/insights/*` are the next concrete piece. |
| Alignment with canonical IA wireframes | Partial | Phase 1 of the IA rename shipped 2026-05-14: route names now match the IA (`/today`, `/insights`, `/cards/[cardId]`, `/review/setup`, etc.), `/profile` is folded into `/settings`, and stub pages exist for every IA surface. Phase 2 progress (verified 2026-05-18): only `/cards/[cardId]/repair` remains as a `StubPage` (`/decks/[id]/preview` shipped 2026-05-18 as a real read-only viewer — see the row above); the Weak spots → Weak spots rename is complete at the route layer (`/insights/weak-spots/*`). Remaining: real implementation behind the repair stub, Settings sections (Display, Data & sync, Review behavior — only `learning/`, `profile/`, `security/` exist under `apps/web/app/(app)/settings/`), and terminology decisions (Listening modality coverage in `03_review_session.md` / `07_generated_card_review.md` / `10_card_detail.md`; the Problem Card title still shows on the repair stub). Tracked on the board under the "IA wireframe doc cleanup", "Reconcile IA card types", "Flesh out IA stub pages", and "Settings IA: missing sections" cards. |
