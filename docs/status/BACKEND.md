# Backend And Data Status

Refreshed by code inspection on 2026-05-14 (Stage 7.1: compliance fix-up on diagnose — Idempotency-Key required, body validated, fetches parallelized, cache key versioned). See [../IMPLEMENTATION_STATUS.md](../IMPLEMENTATION_STATUS.md) for the status legend and summary.

| Capability | Status | Evidence |
|---|---|---|
| Auth signup, cancel-signup, login, refresh, OTP verify/resend, logout | Implemented | `apps/api/src/routes/auth.ts`, `apps/api/src/services/auth.service.ts`, `packages/shared-types/src/schemas/auth.schema.ts` |
| Password change and account deletion | Implemented | `apps/api/src/routes/auth.ts`, `apps/api/src/services/auth.service.ts` |
| Profile get/update with study preferences and interests | Implemented | `apps/api/src/routes/profile.ts`, `apps/api/src/services/profile.service.ts`, `supabase/migrations/20260504000007_normalization_cleanup.sql`, `docs/DATABASE.md` |
| Optimistic concurrency for profile/deck/card PATCH | Implemented | `apps/api/src/controllers/profile.controller.ts`, `apps/api/src/controllers/decks.controller.ts`, `apps/api/src/controllers/cards.controller.ts`, `supabase/migrations/20260525000000_cards_version_column.sql`, `supabase/migrations/20260526000000_decks_profile_version_columns.sql` |
| User deck CRUD and stable pagination | Implemented | `apps/api/src/routes/decks.ts`, `apps/api/src/services/deck.service.ts`, `supabase/migrations/20260522000000_list_decks_and_premade_paginated.sql`, `supabase/migrations/20260523000000_list_pagination_cursor_hardening.sql` |
| Card CRUD, AI/manual creation, sibling shared-field sync | Implemented | `apps/api/src/routes/cards.ts`, `apps/api/src/services/card.service.ts`, `supabase/migrations/20260514000000_atomic_service_writes.sql`, `docs/DATABASE.md` |
| Semantic card similarity and embedding regeneration | Implemented | `apps/api/src/routes/cards.ts`, `apps/api/src/services/card.service.ts`, `apps/api/scripts/backfill-premade-embeddings.ts`, `supabase/migrations/20260502000006_find_similar_cards_rpc.sql`, `supabase/migrations/20260509000000_hnsw_partial_user_id.sql` |
| Premade deck browse, subscription, unsubscribe, self-healing subscribe | Implemented | `apps/api/src/routes/premade.ts`, `apps/api/src/services/premade.service.ts`, `supabase/migrations/20260504000003_subscribe_to_premade_deck_rpc.sql`, `supabase/migrations/20260521000000_subscribe_self_heal_and_orphan_cleanup.sql` |
| Full launch-size curated premade catalogue | Partial | Premade deck records and seed cards exist in `supabase/migrations/20260504000000_seed_premade_decks.sql`, but static inspection shows starter seed content, not the large catalogue described by older planning docs. |
| Premade deck update/merge workflow | Partial | `premade_decks.version` and `user_premade_subscriptions.last_seen_version` exist in `docs/DATABASE.md`; no merge/update route was found in `apps/api/src/routes/premade.ts`. |
| Onboarding deck recommendations API | Missing | `apps/web/app/onboarding/decks/page.tsx` references `/onboarding/recommendations`; no matching route family was found in `apps/api/src/routes/` or `apps/web/app/api/`. |
| FSRS review submit, batch submit, due queue, review forecast | Implemented | `apps/api/src/routes/reviews.ts`, `apps/api/src/services/fsrs.service.ts`, `apps/api/src/services/review.service.ts`, `supabase/migrations/20260515000000_batch_query_rpcs.sql`, `supabase/migrations/20260529000000_learner_timezone_review_buckets.sql`, `supabase/migrations/20260530000000_review_forecast_actual_new_counts.sql` |
| Review session summary including leeches | Implemented | `apps/api/src/routes/reviews.ts`, `apps/api/src/services/review.service.ts`, `supabase/migrations/20260516000000_pagination_and_session_summary_rpcs.sql`, `supabase/migrations/20260519000000_leech_cascade_and_premade_doc.sql` |
| Service-level rollback, forget, and reschedule from history | Implemented | `apps/api/src/services/fsrs.service.ts`, `supabase/migrations/20260502000001_review_logs_before_snapshot.sql`, `supabase/migrations/20260502000003_process_forget_rpc.sql` |
| Public API routes for rollback/forget/reschedule | Implemented | `POST /api/v1/reviews/:reviewLogId/rollback` (`apps/api/src/routes/reviews.ts`), `POST /api/v1/cards/:id/forget`, and `POST /api/v1/cards/:id/reschedule` (`apps/api/src/routes/cards.ts`). All three are `withIdempotency`-wrapped; params/body validated by `rollbackReviewParamSchema` / `forgetCardBodySchema` / `emptyBodySchema` (`.strict()`). Default user limiter only — these are rare manual-recovery operations, not hot-path mutations. Reuses existing service error codes (`CARD_NOT_FOUND`, `REVIEW_LOG_NOT_FOUND`, `PREMADE_CARD_NOT_RESETTABLE`, `ROLLBACK_NOT_AVAILABLE`, `RESCHEDULE_NO_HISTORY`, `RESCHEDULE_NO_RESULT`). Forget optionally accepts `{ resetCount: boolean }`. |
| Leech flagging | Implemented | `apps/api/src/services/fsrs.service.ts`, `supabase/migrations/20260514000000_atomic_service_writes.sql`, `docs/DATABASE.md` |
| Leeches list, detail, resolve, reopen | Implemented | `apps/api/src/routes/leeches.ts`, `apps/api/src/controllers/leeches.controller.ts`, `apps/api/src/services/leech.service.ts`, `apps/api/src/schemas/leech.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechListItemSchema`, `ApiLeechListResponseSchema`). `GET /api/v1/leeches` with status/deck/JLPT/cardType filters, `diagnosis: available\|missing` filter, four sort modes (`mostRecent`, `oldestUnresolved`, `mostLapses`, `deckOrder`) and `(created_at, id)` tuple-cursor pagination on the two time-keyed sorts; `GET /api/v1/leeches/:id` returns joined card+deck context; `POST /api/v1/leeches/:id/resolve` and `/reopen` are idempotent and translate the partial-unique-index 23505 to HTTP 409 `LEECH_ALREADY_OPEN`. The paid `diagnosis: 'not included in plan'` arm is omitted until entitlements ship. |
| Leech drill session creation + snapshot + lifecycle | Implemented | `POST /api/v1/leeches/drill-sessions` (Stage 3) + `POST /:sessionId/finish` + `POST /:sessionId/abort` (Stage 6) — `apps/api/src/routes/leeches.ts`, `apps/api/src/controllers/leeches.controller.ts`, `apps/api/src/services/leech.service.ts`, `apps/api/src/schemas/leech.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechDrillSessionSchema`). Stage 3 (`20260531000000_leech_drill_sessions.sql`) creates the two tables + SECURITY DEFINER RPC `create_leech_drill_session`. Stage 6 (`20260603000000_drill_session_lifecycle_and_sources.sql`) extends the create RPC to a four-branch `UNION ALL` candidate CTE so all five spec source values (`unresolvedLeeches`, `deckScoped`, `highLapseCandidates`, `manualSelection`, `currentCard`) are wired through, and adds the `transition_leech_drill_session` RPC for `active → finished` / `active → aborted` transitions (idempotent on no-op retries, 409 `LEECH_DRILL_SESSION_STATE_CONFLICT` on illegal transitions). `Idempotency-Key` is required on create. Service does not import `fsrs.service.ts`. |
| Leech drill session resume + stale detection | Implemented | `GET /api/v1/leeches/drill-sessions/:sessionId` (Stage 4) — `apps/api/src/routes/leeches.ts`, `apps/api/src/controllers/leeches.controller.ts`, `apps/api/src/services/leech.service.ts`. Migration `20260601000000_leech_drill_session_resume.sql` extracts the fingerprint formula into the `IMMUTABLE` helper `compute_card_state_fingerprint_v1` (used by both create and resume — see DATABASE.md for the byte-for-byte compatibility note + self-test), then adds the SECURITY DEFINER RPC `get_leech_drill_session` which returns the envelope + per-row `isStale`/`isOrphaned` flags + top-level `isCanonicalStateStale`/`staleCards`. Reads `cards` only — no writes. Service translates SQLSTATE 02000 + `leech_drill_session_not_found` to HTTP 404 `LEECH_DRILL_SESSION_NOT_FOUND`. |
| AI leech diagnosis | Implemented | `POST /api/v1/leeches/:id/diagnose` (Stage 7, hardened in Stage 7.1) — `apps/api/src/routes/leeches.ts`, `apps/api/src/controllers/leeches.controller.ts`, `apps/api/src/services/leech.service.ts` (`diagnoseLeech`), `apps/api/src/services/ai.service.ts` (`generateLeechDiagnosis`), `packages/shared-types/src/schemas/ai.schema.ts` (`GeneratedLeechDiagnosisSchema`). Builds prompt context from card content + recent review-log ratings + lapse count + learner profile (JLPT target, native language). Stage 7.1 added: required `Idempotency-Key` header via `withIdempotency`, strict-empty body validation via the renamed `emptyBodySchema`, parallel `Promise.all` fetch of profile + review_logs, and a `DIAGNOSIS_PROMPT_VERSION` constant baked into the cache key so prompt-template edits invalidate cleanly. Replay-on-existing: leeches already populated with diagnosis return cached text without re-calling OpenAI. Gated by auth + AI rate limiters only (no tier check). Writes only `leeches.diagnosis` + `leeches.prescription`; never touches `cards` or `review_logs`. 422 `CARD_FIELDS_INSUFFICIENT` for orphan leeches and sentence-layout cards. |
| Leech drill attempts persistence | Implemented | `POST /api/v1/leeches/drill-sessions/:sessionId/attempts` (Stage 5) — `apps/api/src/routes/leeches.ts`, `apps/api/src/controllers/leeches.controller.ts`, `apps/api/src/services/leech.service.ts`, `apps/api/src/schemas/leech.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechDrillAttemptSchema`). Migration `20260602000000_leech_drill_attempts.sql` adds the `leech_drill_attempts` table with the anti-fraud composite FK `FOREIGN KEY (session_card_id, session_id) REFERENCES leech_drill_session_cards`, plus the `UNIQUE (user_id, event_id)` that makes eventId the structural idempotency identifier. SECURITY DEFINER RPC `record_leech_drill_attempt` validates session-card ownership, checks optional body-side cardId/leechId consistency assertions, and uses `INSERT ... ON CONFLICT (user_id, event_id) DO NOTHING` + replay-fetch for exactly-once delivery. Two new error codes (404 `LEECH_DRILL_SESSION_CARD_NOT_FOUND`, 422 `LEECH_DRILL_ATTEMPT_ASSERTION_MISMATCH`). Property-based scheduler-invariance test suite (200 randomized iterations across all three drill endpoints) proves the drill code path never reads or writes `cards` or `review_logs`. |
| Dashboard `hasLeeches` signal | Missing | `apps/web/app/(app)/dashboard/_components/dashboard-client.tsx` still references a `hasLeeches` flag; the new `GET /api/v1/leeches` could power it but the dashboard wiring has not been updated yet. |
| Analytics heatmap, accuracy, JLPT gap, milestones, bundled dashboard | Implemented | `apps/api/src/routes/analytics.ts`, `apps/api/src/services/analytics.service.ts`, `supabase/migrations/20260503000003_analytics_rpcs.sql`, `supabase/migrations/20260529000000_learner_timezone_review_buckets.sql`, `supabase/migrations/20260604000000_remove_legacy_streaks.sql`. Bundled dashboard returns four sections (heatmap, accuracy, jlptGap, milestones); legacy streak surface removed end-to-end in Stage 8. |
| Dashboard deck rollup API | Missing | `GET /api/v1/decks` provides deck names and card counts for the dashboard, but due/new/review/mastery/last-reviewed rollups are not exposed as a dashboard-specific contract. |
| Paid/free tier entitlement model | N/A | Stage 7 (commit pending) removed the tier model entirely. For the MVP release, every Tomo feature is available to every authenticated learner. AI endpoints are gated only by auth + cost-control rate limiters (per-minute, per-day). Any future monetization decision will be a separate, post-MVP product decision. |
| Idempotency for retryable mutating requests | Implemented | `apps/api/src/lib/idempotency.ts`, controller usage in decks/cards/reviews/premade, `supabase/migrations/20260524000000_idempotency_keys.sql` |
| Production-only rate limiting with fail-open infrastructure behavior | Implemented | `apps/api/src/middleware/rateLimit.ts`, `apps/api/src/lib/circuit-breaker.ts`; dev/test bypass the limiter path. |
| Structured request logging and graceful shutdown | Implemented | `apps/api/src/middleware/requestLogger.ts`, `apps/api/src/lib/logger.ts`, `apps/api/src/lib/shutdown.ts`, `apps/api/src/routes/health.ts` |
