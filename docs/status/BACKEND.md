# Backend And Data Status

Refreshed by code inspection on 2026-05-18 (reconciliation pass). The 2026-05-17 refresh left two rows with stale `Missing` labels that contradicted later kanban decisions; this pass corrects them. Specifically: **Onboarding deck recommendations API** is reclassified `N/A` (the 2026-05-17 kanban decided no endpoint is needed — every recommender signal is already exposed via `/api/v1/premade-decks` + `/api/v1/profile`), and **Today `hasLeeches` signal** is reclassified `Implemented` (served by the existing `GET /api/v1/weak-spots?status=unresolved` endpoint; the missing piece is the frontend Today-page surface, tracked in [FRONTEND.md](FRONTEND.md) and the kanban's Weak spots Phase 3 item). Prior 2026-05-17 refresh note: post-rename verification pass that corrected stale `/dashboard` → `/today` references. See [../IMPLEMENTATION_STATUS.md](../IMPLEMENTATION_STATUS.md) for the status legend and summary.

| Capability | Status | Evidence |
|---|---|---|
| Auth signup, cancel-signup, login, refresh, OTP verify/resend, logout | Implemented | `apps/api/src/routes/auth.ts`, `apps/api/src/services/auth.service.ts`, `packages/shared-types/src/schemas/auth.schema.ts` |
| Password change and account deletion | Implemented | `apps/api/src/routes/auth.ts`, `apps/api/src/services/auth.service.ts` |
| Profile get/update with study preferences and interests | Implemented | `apps/api/src/routes/profile.ts`, `apps/api/src/services/profile.service.ts`, `supabase/migrations/20260504000007_normalization_cleanup.sql`, `docs/DATABASE.md` |
| Optimistic concurrency for profile/deck/card PATCH | Implemented | `apps/api/src/controllers/profile.controller.ts`, `apps/api/src/controllers/decks.controller.ts`, `apps/api/src/controllers/cards.controller.ts`, `supabase/migrations/20260525000000_cards_version_column.sql`, `supabase/migrations/20260526000000_decks_profile_version_columns.sql` |
| User deck CRUD and stable pagination | Implemented | `apps/api/src/routes/decks.ts`, `apps/api/src/services/deck.service.ts`, `supabase/migrations/20260522000000_list_decks_and_premade_paginated.sql`, `supabase/migrations/20260523000000_list_pagination_cursor_hardening.sql` |
| Card CRUD, AI/manual creation, sibling shared-field sync | Implemented | `apps/api/src/routes/cards.ts`, `apps/api/src/services/card.service.ts`, `supabase/migrations/20260514000000_atomic_service_writes.sql`, `docs/DATABASE.md` |
| Semantic card similarity and embedding regeneration | Implemented | `apps/api/src/routes/cards.ts`, `apps/api/src/services/card.service.ts`, `apps/api/scripts/backfill-premade-embeddings.ts`, `supabase/migrations/20260502000006_find_similar_cards_rpc.sql`, `supabase/migrations/20260509000000_hnsw_partial_user_id.sql` |
| Premade deck browse, copy to library | Implemented | **Updated 2026-05-17 (Backend Completion Plan Stage 4 rewrite shipped — copy model):** browse and copy are both live. Migration `supabase/migrations/20260607000000_premade_copy_model.sql` dropped `user_premade_subscriptions`, `decks.is_premade_fork`, `premade_decks.version` + its CHECK, and the two old subscribe/unsubscribe RPCs; replaced with `copy_premade_deck(p_user_id, p_premade_deck_id)` (SECURITY DEFINER, pinned `search_path = ''`, grants EXECUTE to service_role). Route is `POST /api/v1/premade-decks/:id/copy` (idempotent via `withIdempotency`, empty body, reuses the existing per-user lockout from `subscribeRateLimitMiddleware`). Allows duplicates by design — refreshing content means deleting the deck and copying again (FSRS-progress cost is explicit). Cards land with fresh FSRS state and inherit `embedding` + `embedding_updated_at` so similarity search works day 1. Service: `apps/api/src/services/premade.service.ts::copyPremadeDeck` is the only mutator now; `apps/api/src/services/deck.service.ts::deleteDeck` no longer special-cases premade forks. Wire shape: `ApiPremadeSubscriptionSchema` and `ApiSubscribeResultSchema` removed; `ApiCopyPremadeDeckResultSchema = { deckId, cardCount }` added; `isPremadeFork` dropped from `ApiDeckSchema`. Coverage: unit suite in `apps/api/src/services/__tests__/premade.service.test.ts` (success / 404 / empty RPC / duplicate / dbError) plus integration suite in `apps/api/tests/integration/premade.routes.test.ts` covering the four plan acceptance criteria (fresh copy → 201 + Location, duplicate copy → two independent decks, copy → delete → copy-again, inactive deck → 404). Frontend visual rebranding ("Subscribe" → "Add to my library", attribution chip, unified delete-confirmation copy) tracked as a separate kanban follow-up. **Corrective migration 2026-05-18:** Stage 4's `premade_decks.version` DROP left `list_premade_decks_paginated` (defined in `20260523000000_list_pagination_cursor_hardening.sql`) referencing `pd.version` in both `RETURNS TABLE` and the SELECT — PL/pgSQL bodies escape Postgres dependency tracking, so the DROP applied cleanly while leaving the RPC broken at the next call (`column pd.version does not exist`, SQLSTATE 42703). This wedged `GET /api/v1/premade-decks` (the route returned 500), which in turn rendered `/decks/premade` as the empty state because `apiCallSafe` swallows 5xx into its fallback. Migration `supabase/migrations/20260616000000_list_premade_decks_paginated_drop_version.sql` redefines the RPC without `version`. Service/wire layer already matched the post-fix shape (`apps/api/src/services/premade.service.ts:46–59`; `packages/shared-types/src/schemas/api.schema.ts:128–142`); no app-code changes were required. Same class of bug as `20260613000000`. |
| Full launch-size curated premade catalogue | Partial | Premade deck records and seed cards exist in `supabase/migrations/20260504000000_seed_premade_decks.sql`, but static inspection shows starter seed content, not the large catalogue described by older planning docs. |
| Premade deck update/merge workflow | Removed from scope | **Decision recorded 2026-05-17 (executed in the same date's migration):** premade decks are *starting points, not strict paths*. Users copy a premade deck once via `copy_premade_deck` (Backend Completion Plan Stage 4 rewrite — shipped); there is no ongoing subscription, no version-drift tracking, no merge/sync workflow. To refresh content, the user deletes their deck and copies again — the FSRS-progress cost is surfaced explicitly. The read-only `version`/`lastSeenVersion` fields and the `user_premade_subscriptions` table have been removed by `supabase/migrations/20260607000000_premade_copy_model.sql`. See [DATABASE.md](../DATABASE.md) "Key Design Decisions → Premade decks are a starting point, not a strict path." |
| Onboarding deck recommendations API | N/A | **Reclassified 2026-05-18 to match the 2026-05-17 kanban decision:** no backend endpoint is required. Every signal a client-side recommender needs is already exposed — `premade_decks.{jlpt_level, domain, deck_type}` ship via `GET /api/v1/premade-decks` and `profiles.{jlpt_target, interests}` ship via `GET /api/v1/profile`. The remaining work is a frontend slice (replace the hardcoded `RECOMMENDED_DECKS` in `apps/web/app/onboarding/decks/page.tsx:33-37` with client-side scoring + `POST /api/v1/premade-decks/:id/copy`) tracked on the kanban as **Onboarding deck recommendations (frontend slice)**. The stale `/onboarding/recommendations` URL in the page's TODO comment is being removed in that same slice. |
| FSRS review submit, batch submit, due queue, review forecast | Implemented | `apps/api/src/routes/reviews.ts`, `apps/api/src/services/fsrs.service.ts`, `apps/api/src/services/review.service.ts`, `supabase/migrations/20260515000000_batch_query_rpcs.sql`, `supabase/migrations/20260529000000_learner_timezone_review_buckets.sql`, `supabase/migrations/20260530000000_review_forecast_actual_new_counts.sql` |
| Review session summary including weak spots | Implemented | `apps/api/src/routes/reviews.ts`, `apps/api/src/services/review.service.ts`, `supabase/migrations/20260516000000_pagination_and_session_summary_rpcs.sql`, `supabase/migrations/20260519000000_leech_cascade_and_premade_doc.sql` |
| Service-level rollback, forget, and reschedule from history | Implemented | `apps/api/src/services/fsrs.service.ts`, `supabase/migrations/20260502000001_review_logs_before_snapshot.sql`, `supabase/migrations/20260502000003_process_forget_rpc.sql` |
| Public API routes for rollback/forget/reschedule | Implemented | `POST /api/v1/reviews/:reviewLogId/rollback` (`apps/api/src/routes/reviews.ts`), `POST /api/v1/cards/:id/forget`, and `POST /api/v1/cards/:id/reschedule` (`apps/api/src/routes/cards.ts`). All three are `withIdempotency`-wrapped; params/body validated by `rollbackReviewParamSchema` / `forgetCardBodySchema` / `emptyBodySchema` (`.strict()`). Default user limiter only — these are rare manual-recovery operations, not hot-path mutations. Reuses existing service error codes (`CARD_NOT_FOUND`, `REVIEW_LOG_NOT_FOUND`, `PREMADE_CARD_NOT_RESETTABLE`, `ROLLBACK_NOT_AVAILABLE`, `RESCHEDULE_NO_HISTORY`, `RESCHEDULE_NO_RESULT`). Forget optionally accepts `{ resetCount: boolean }`. |
| Insights Statistics distributions + dashboard extensions | Implemented | **Shipped 2026-05-18.** New `GET /api/v1/insights/distributions` returns a bundled `{ ratings, intervals, stability, difficulty }` envelope (rating histogram + three FSRS-state histograms). Backed by four new SECURITY DEFINER RPCs in `supabase/migrations/20260618000000_insights_distributions_and_dashboard_extensions.sql`: `get_answer_rating_distribution`, `get_interval_distribution`, `get_stability_distribution`, `get_difficulty_distribution`. Bucket boundaries are server-defined (TEXT labels on the wire) so the frontend doesn't have to re-derive them. The same migration also: (a) adds `get_cards_added_this_month(p_user_id, p_timezone)` for the Progress page; (b) extends `get_heatmap_data` to project per-day `total_seconds` (SUM(`review_logs.review_time_ms`)/1000) so the Statistics activity strip can drop its 18-second heuristic; (c) bundles `cards_added_this_month` into the dashboard envelope; (d) consolidates the accidental dual overload of `get_heatmap_data` and `get_dashboard_data` (UUID) vs (UUID, TEXT) into a single canonical `(UUID, TEXT DEFAULT 'UTC')` signature each. Backed by 26 service tests in `insights.service.test.ts` (zero-fill rating, error bubble, tz-forward for cards-added). |
| Cross-deck cards browser endpoints (`/cards` page) | Implemented | **Shipped 2026-05-18.** New endpoints under `/api/v1/cards`: `GET /cross-deck` (`crossDeckListCardsQuerySchema` — limit/cursor/search/deckId/jlptLevel/status/missingField/sort) → `{ items: ApiCrossDeckCardListItem[], nextCursor, hasMore, totalCount }`; `POST /:id/move`, `POST /:id/copy`, `POST /:id/suspend`, `POST /:id/unsuspend` (`Idempotency-Key` required; each returns the affected `ApiCard`); bulk endpoints `POST /bulk/{move,suspend,unsuspend,delete,tag}` (each returns `ApiBulkCardMutationResult = { succeeded: string[], failed: { id, error, code? }[] }` so partial success is structured). Migrations `20260617000000_list_cards_cross_deck_rpc.sql` (`list_cards_cross_deck`, `count_cards_cross_deck`) and `20260617000001_card_move_copy_suspend_rpcs.sql` (`move_card`, `copy_card`, `suspend_card`, `unsuspend_card`, `bulk_move_cards`, `bulk_suspend_cards`, `bulk_unsuspend_cards`, `bulk_delete_cards`, `bulk_tag_cards`) carry nine new RPCs — each `SECURITY DEFINER` + `SET search_path = ''` + explicit `GRANT EXECUTE ... TO service_role` per the standing rule. Premade source cards (`user_id IS NULL`) are rejected on every mutating path with the same shape as a missing-row 404. `move_card` adjusts `decks.card_count` on both source and target inline (the `cards_count_trigger` only fires on INSERT/DELETE). Cross-deck list joins `decks` for `deck_name` and projects `lapses` so the table renders without an N+1 deck lookup. Backed by `apps/api/src/services/__tests__/card.service.test.ts` (14 tests: list filters, move + premade rejection, bulk-mutation partition shape, tag set algebra defaults). |
| Weak spot flagging | Implemented | `apps/api/src/services/fsrs.service.ts`, `supabase/migrations/20260514000000_atomic_service_writes.sql`, `docs/DATABASE.md` |
| Weak spots list, detail, resolve, reopen | Implemented | `apps/api/src/routes/weak spots.ts`, `apps/api/src/controllers/weak spots.controller.ts`, `apps/api/src/services/weak spot.service.ts`, `apps/api/src/schemas/weak spot.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechListItemSchema`, `ApiLeechListResponseSchema`). `GET /api/v1/weak-spots` with status/deck/JLPT/cardType filters, `diagnosis: available\|missing` filter, four sort modes (`mostRecent`, `oldestUnresolved`, `mostLapses`, `deckOrder`) and `(created_at, id)` tuple-cursor pagination on the two time-keyed sorts; `GET /api/v1/weak-spots/:id` returns joined card+deck context; `POST /api/v1/weak-spots/:id/resolve` and `/reopen` are idempotent and translate the partial-unique-index 23505 to HTTP 409 `WEAK_SPOT_ALREADY_OPEN`. The paid `diagnosis: 'not included in plan'` arm is omitted until entitlements ship. |
| Weak spot drill session creation + snapshot + lifecycle | Implemented | `POST /api/v1/weak-spots/drill-sessions` (Stage 3) + `POST /:sessionId/finish` + `POST /:sessionId/abort` (Stage 6) — `apps/api/src/routes/weak spots.ts`, `apps/api/src/controllers/weak spots.controller.ts`, `apps/api/src/services/weak spot.service.ts`, `apps/api/src/schemas/weak spot.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechDrillSessionSchema`). Stage 3 (`20260531000000_weak_spot_drill_sessions.sql`) creates the two tables + SECURITY DEFINER RPC `create_weak_spot_drill_session`. Stage 6 (`20260603000000_drill_session_lifecycle_and_sources.sql`) extends the create RPC to a four-branch `UNION ALL` candidate CTE so all five spec source values (`unresolvedLeeches`, `deckScoped`, `highLapseCandidates`, `manualSelection`, `currentCard`) are wired through, and adds the `transition_weak_spot_drill_session` RPC for `active → finished` / `active → aborted` transitions (idempotent on no-op retries, 409 `LEECH_DRILL_SESSION_STATE_CONFLICT` on illegal transitions). `Idempotency-Key` is required on create. Service does not import `fsrs.service.ts`. |
| Weak spot drill session resume + stale detection | Implemented | `GET /api/v1/weak-spots/drill-sessions/:sessionId` (Stage 4) — `apps/api/src/routes/weak spots.ts`, `apps/api/src/controllers/weak spots.controller.ts`, `apps/api/src/services/weak spot.service.ts`. Migration `20260601000000_leech_drill_session_resume.sql` extracts the fingerprint formula into the `IMMUTABLE` helper `compute_card_state_fingerprint_v1` (used by both create and resume — see DATABASE.md for the byte-for-byte compatibility note + self-test), then adds the SECURITY DEFINER RPC `get_weak_spot_drill_session` which returns the envelope + per-row `isStale`/`isOrphaned` flags + top-level `isCanonicalStateStale`/`staleCards`. Reads `cards` only — no writes. Service translates SQLSTATE 02000 + `leech_drill_session_not_found` to HTTP 404 `WEAK_SPOT_DRILL_SESSION_NOT_FOUND`. |
| AI weak-spot diagnosis | Implemented | `POST /api/v1/weak-spots/:id/diagnose` (Stage 7, hardened in Stage 7.1) — `apps/api/src/routes/weak spots.ts`, `apps/api/src/controllers/weak spots.controller.ts`, `apps/api/src/services/weak spot.service.ts` (`diagnoseLeech`), `apps/api/src/services/ai.service.ts` (`generateLeechDiagnosis`), `packages/shared-types/src/schemas/ai.schema.ts` (`GeneratedLeechDiagnosisSchema`). Builds prompt context from card content + recent review-log ratings + lapse count + learner profile (JLPT target, native language). Stage 7.1 added: required `Idempotency-Key` header via `withIdempotency`, strict-empty body validation via the renamed `emptyBodySchema`, parallel `Promise.all` fetch of profile + review_logs, and a `DIAGNOSIS_PROMPT_VERSION` constant baked into the cache key so prompt-template edits invalidate cleanly. Replay-on-existing: weak spots already populated with diagnosis return cached text without re-calling OpenAI. Gated by auth + AI rate limiters only (no tier check). Writes only `weak spots.diagnosis` + `weak spots.prescription`; never touches `cards` or `review_logs`. 422 `CARD_FIELDS_INSUFFICIENT` for orphan weak spots and sentence-layout cards. |
| Weak spot drill attempts persistence | Implemented | `POST /api/v1/weak-spots/drill-sessions/:sessionId/attempts` (Stage 5) — `apps/api/src/routes/weak spots.ts`, `apps/api/src/controllers/weak spots.controller.ts`, `apps/api/src/services/weak spot.service.ts`, `apps/api/src/schemas/weak spot.schema.ts`, `packages/shared-types/src/schemas/api.schema.ts` (`ApiLeechDrillAttemptSchema`). Migration `20260602000000_weak_spot_drill_attempts.sql` adds the `weak_spot_drill_attempts` table with the anti-fraud composite FK `FOREIGN KEY (session_card_id, session_id) REFERENCES weak_spot_drill_session_cards`, plus the `UNIQUE (user_id, event_id)` that makes eventId the structural idempotency identifier. SECURITY DEFINER RPC `record_weak_spot_drill_attempt` validates session-card ownership, checks optional body-side cardId/leechId consistency assertions, and uses `INSERT ... ON CONFLICT (user_id, event_id) DO NOTHING` + replay-fetch for exactly-once delivery. Two new error codes (404 `LEECH_DRILL_SESSION_CARD_NOT_FOUND`, 422 `WEAK_SPOT_DRILL_ATTEMPT_ASSERTION_MISMATCH`). Property-based scheduler-invariance test suite (200 randomized iterations across all three drill endpoints) proves the drill code path never reads or writes `cards` or `review_logs`. |
| Today `hasLeeches` signal | Implemented | **Reclassified 2026-05-18:** the backend signal is fully served by the existing `GET /api/v1/weak-spots?status=unresolved` endpoint (`apps/api/src/routes/weak spots.ts:15`). The frontend already consumes it via `useUnresolvedLeechCount` (`apps/web/lib/api/weak spots.ts:92`) for the sidebar count badge (`apps/web/app/(app)/_components/nav-item.tsx:429`). The remaining work is a frontend surface — a weak-spots card on `/today` — tracked on the kanban under **Weak spots Phase 3 — drill follow-ups** and mirrored in [status/FRONTEND.md](FRONTEND.md) under the Today live-data row. No new endpoint required. |
| Insights — confusable-pair detection | Implemented | **Backend Completion Plan Stage 10 shipped 2026-05-17:** `GET /api/v1/insights/confusable-pairs?limit=…` returns the user's top mis-rated semantically-similar card pairs. Migration `supabase/migrations/20260611000000_confusable_pairs.sql` adds: `confusable_pairs` table (PK `(user_id, card_a, card_b)` + `CHECK (card_a < card_b)` for schema-level idempotency, partial index on `(user_id, miss_count DESC, similarity_score DESC)`, RLS read-own); `record_confusable_pairs()` SECURITY DEFINER detection (one INSERT, CTE pipeline, MISS_COUNT_THRESHOLD=2, SIMILARITY_THRESHOLD=0.70); `get_confusable_pairs(p_user_id, p_limit)` reader RPC; defensive `cron.schedule('record_confusable_pairs_daily', '0 3 * * *', …)` wrapped in `DO`-block. Service `apps/api/src/services/insights.service.ts::listConfusablePairs` + controller `confusablePairs` + route on `/api/v1/insights`. Wire: `ApiConfusablePairSchema` with nested cardA/cardB display fields. **Operations:** pg_cron must be enabled in Supabase dashboard for the daily detection job to run; without it, the pair table never populates but the RPC + table still work against any manual `SELECT record_confusable_pairs()` invocation. Coverage: 5 unit + 8 integration tests including idempotency (twice-running detection produces same row count) and threshold negatives (single-session co-mis-rate, orthogonal embeddings). |
| Insights — maturity-pipeline history (per-day snapshots) | Implemented | **Backend Completion Plan Stage 9 shipped 2026-05-17:** `GET /api/v1/insights/maturity-history?days=90\|180\|365` returns the user's per-state card counts (new/learning/review/relearning/mature) per day for the requested window. Migration `supabase/migrations/20260610000000_card_state_snapshots.sql` adds: `card_state_snapshots` table (PK `(user_id, snapshot_date)` + RLS); `record_card_state_snapshots()` SECURITY DEFINER cron function — one upsert per profile, FILTER aggregates per FSRS state; `get_maturity_pipeline_history(p_user_id, p_days)` RPC — reads historical rows from the snapshot table, computes today live from `cards` so the chart reflects the current moment between cron runs; defensive `cron.schedule('record_card_state_snapshots_daily', '15 2 * * *', …)` wrapped in `DO`-block-guard so the migration applies cleanly on projects without pg_cron enabled. Suspended cards excluded. Mature = state 2 + scheduled_days≥21 (Anki convention; matches Stage 3's predicate). Service `apps/api/src/services/insights.service.ts::listMaturityHistory`; controller + route on `/api/v1/insights`. Wire types: `ApiMaturitySnapshotSchema` + `ApiMaturityHistoryDaysSchema`. **Operations:** pg_cron must be enabled in the Supabase dashboard for historical rows to accumulate (the application works without it — the RPC always computes today live). Coverage: 6 unit + 7 integration tests including a manual cron-simulation upsert round-trip. |
| Insights — card-quality issue counts | Implemented | **Backend Completion Plan Stage 8 shipped 2026-05-17:** `GET /api/v1/insights/card-quality` returns six rows of `{ issueType, count }` (`missing_reading`, `missing_meaning`, `missing_example`, `missing_mnemonic`, `missing_picture`, `missing_nuance`) over the user's vocabulary+grammar cards. Sentence-layout cards excluded. Migration `supabase/migrations/20260609000000_get_card_quality_issues_rpc.sql` adds SECURITY DEFINER RPC `get_card_quality_issues(p_user_id)`; single-scan FILTER aggregates + LATERAL VALUES unpivot. The six rows are always present in the response even when zero — stable shape for consumers. Service `apps/api/src/services/insights.service.ts::listCardQualityIssues` + controller `cardQuality` + `GET /card-quality` on the existing `/api/v1/insights` mount. Wire: `ApiCardQualityIssueSchema` + `ApiCardQualityIssueTypeSchema`. Frontend `cards-quality-bars.tsx` enum mismatch is a tracked follow-up. |
| Insights — problem-card list (lapse-bucketed) | Implemented | **Backend Completion Plan Stage 7 shipped 2026-05-17:** `GET /api/v1/insights/problem-cards?bucket=…` returns the user's non-suspended cards in one of `'2-3' \| '4-5' \| '6-7' \| '8plus'`. Migration `supabase/migrations/20260608000000_get_problem_cards_rpc.sql` adds the SECURITY DEFINER RPC `get_problem_cards(p_user_id, p_bucket)`; route mounted at `/api/v1/insights` behind auth + default rate-limiter; service `apps/api/src/services/insights.service.ts`; controller `apps/api/src/controllers/insights.controller.ts`; query validation `apps/api/src/schemas/insights.schema.ts`. Wire shape `ApiProblemCardSchema` + `ApiProblemCardBucketSchema` in `packages/shared-types/src/schemas/api.schema.ts`. The 8plus bucket cardinality equals the unresolved-weak spot count for the same user (process_review inserts a weak spot at lapses ≥ WEAK_SPOT_THRESHOLD). The `/insights/mistakes` page that originally consumed this endpoint was retired in the 2026-05-17 IA restructure — the endpoint ships consumer-agnostic for future surfaces (a `/cards` lapse-range saved view, a Stage 8/9 analytics histogram). |
| Analytics heatmap, accuracy, JLPT gap, milestones, bundled dashboard | Implemented | `apps/api/src/routes/analytics.ts`, `apps/api/src/services/analytics.service.ts`, `supabase/migrations/20260503000003_analytics_rpcs.sql`, `supabase/migrations/20260529000000_learner_timezone_review_buckets.sql`, `supabase/migrations/20260604000000_remove_legacy_streaks.sql`. Bundled dashboard returns four sections (heatmap, accuracy, jlptGap, milestones); legacy streak surface removed end-to-end in Stage 8. |
| Dashboard deck rollup API | Implemented | **Updated 2026-05-17 (Backend Completion Plan Stage 3 shipped):** `GET /api/v1/decks` now returns per-deck `dueCount`, `newCount`, `matureCount`, `dueNewCount`, `dueReviewCount`, and `lastReviewedAt` rollups in one round-trip — eliminates the N+1 fanout the dashboard would otherwise need. Migration `supabase/migrations/20260606000000_list_decks_paginated_rollups.sql` extends the `list_decks_paginated` RPC's `RETURNS TABLE` signature with the six new columns and computes them via a CTE-based single-pass aggregate (`COUNT(*) FILTER` for the buckets, `MAX(cards.last_review)` for the timestamp). `ApiDeckWithStatsSchema` gains `lastReviewedAt: string | null`; `listDecks` return type changes from `ApiList<ApiDeck>` to `ApiList<ApiDeckWithStats>`. `getDeck` gains one parallel `MAX(last_review)` query so the detail endpoint matches the same shape. Frontend N+1 elimination (`apps/web/app/(app)/decks/_components/deck-list.tsx`'s `useQueries` fanout to `getDeckAction`) tracked as a follow-up — the wire change is the deliverable. **Corrective migration 2026-05-18:** Stage 3's RPC body referenced `decks.is_premade_fork`, which Stage 4 (`20260607000000_premade_copy_model.sql`) then dropped — PL/pgSQL bodies escape Postgres dependency tracking, so the column DROP applied cleanly while leaving the RPC broken at the next call. Migration `supabase/migrations/20260613000000_list_decks_paginated_drop_premade_fork.sql` redefines `list_decks_paginated` without `is_premade_fork` in the `RETURNS TABLE` signature, the `page_decks` CTE projection, and the outer `SELECT`. Service/wire layer already matched the post-fix shape (`apps/api/src/services/deck.service.ts:22, 51`; `packages/shared-types/src/schemas/api.schema.ts:102`); no app-code changes were required. |
| Paid/free tier entitlement model | N/A | Stage 7 (commit pending) removed the tier model entirely. For the MVP release, every Tomo feature is available to every authenticated learner. AI endpoints are gated only by auth + cost-control rate limiters (per-minute, per-day). Any future monetization decision will be a separate, post-MVP product decision. |
| Idempotency for retryable mutating requests | Implemented | `apps/api/src/lib/idempotency.ts`, controller usage in decks/cards/reviews/premade, `supabase/migrations/20260524000000_idempotency_keys.sql`. The premade idempotency wrapping moves from `POST .../subscribe` to `POST .../copy` under the Stage 4 copy-model migration. |
| Production-only rate limiting with fail-open infrastructure behavior | Implemented | `apps/api/src/middleware/rateLimit.ts`, `apps/api/src/lib/circuit-breaker.ts`; dev/test bypass the limiter path. |
| Structured request logging and graceful shutdown | Implemented | `apps/api/src/middleware/requestLogger.ts`, `apps/api/src/lib/logger.ts`, `apps/api/src/lib/shutdown.ts`, `apps/api/src/routes/health.ts` |
