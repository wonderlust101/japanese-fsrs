# Implementation Status

This is the current status index. Detailed implementation evidence is split by area under [status/](status/).

Refreshed by code inspection on 2026-05-17 (post-rename verification pass). Confirmed against the working tree: the Leeches → Weak spots route rename is complete (`apps/web/app/(app)/insights/weak-spots/{page.tsx, drill/setup/, drill/[sessionId]/, drill/[sessionId]/summary/}`), only two `StubPage` placeholders remain (`/cards/[cardId]/repair`, `/decks/[id]/preview`), `lucide-react` is import-free across `apps/web/{app,components,lib}` (single comment hit in `arrow-glyph.tsx`), no `loading.tsx` files exist anywhere under `apps/web/app/`, and the landing page (`apps/web/app/page.tsx`) is still a 7-line redirect to `/onboarding`.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 1 — Lapis-style schema admission**: the five additive review-UI fields (`picture`, `expressionAudio`, `pitchPosition`, `nuance` on `WordFieldsSchema`; `sentenceAudio` on `ExampleSentenceSchema`) now exist in `packages/shared-types/src/schemas/field-shapes.schema.ts` as `.nullable().optional()` keys, with `pitchPosition` constrained to integer ≥ 0. No migration was needed — the `cards_fields_data_shape` CHECK constraint (`supabase/migrations/20260504000007_normalization_cleanup.sql`) only enforces *required* keys for vocabulary/grammar (`word`, `reading`, `meaning`) and does not whitelist additional keys. Round-trip coverage shipped in `packages/shared-types/src/schemas/__tests__/field-shapes.schema.test.ts` and `apps/api/tests/integration/cards.routes.test.ts`.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 2 — Lapis-style AI population**: `GeneratedCardDataSchema` in `packages/shared-types/src/schemas/ai.schema.ts` now admits all five Lapis keys (so the structured-output `.parse()` does not strip them), and the `generateCard` prompt in `apps/api/src/services/ai.service.ts` asks the model for `pitchPosition` + `nuance` while explicitly forbidding it from inventing URLs for the three asset fields. `CARD_PROMPT_VERSION = 'v2'` is baked into the Redis cache key so pre-Stage-2 cache entries are bypassed forever and TTL out naturally (mirroring `DIAGNOSIS_PROMPT_VERSION`). Six new unit tests in `apps/api/src/services/__tests__/ai.service.test.ts` cover round-trip, cache-version bypass, schema admission for `sentenceAudio` + `picture` + `expressionAudio`, and rejection of bad `pitchPosition` values.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 3 — Dashboard deck rollup batch**: `GET /api/v1/decks` returns per-deck rollups (`dueCount`, `newCount`, `matureCount`, `dueNewCount`, `dueReviewCount`, `lastReviewedAt`) in one round-trip — eliminates the N+1 fanout the dashboard previously needed. Migration `supabase/migrations/20260606000000_list_decks_paginated_rollups.sql` extends `list_decks_paginated`'s `RETURNS TABLE` with six new columns and computes them via a CTE-based single-pass aggregate (`COUNT(*) FILTER` for the buckets, `MAX(cards.last_review)` for the timestamp). `ApiDeckWithStatsSchema` gains `lastReviewedAt: string | null`; `listDecks` return type changes from `ApiList<ApiDeck>` to `ApiList<ApiDeckWithStats>`. `getDeck` gains one parallel `MAX(last_review)` query so the detail endpoint matches. Integration tests in `apps/api/tests/integration/decks.routes.test.ts` updated to assert the new wire shape (including the empty-deck zero/null path) plus a new test that creates two cards and verifies populated rollups land on `/decks` in a single request.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 4 — Surface premade `version` + `lastSeenVersion`**: read-only contract change. `GET /api/v1/premade-decks/subscriptions/me` now returns each subscription with `version` (current `premade_decks.version`) and `lastSeenVersion` (the subscriber's `user_premade_subscriptions.last_seen_version`). The pair powers the frontend's "new content available" gate without any write path yet — Stage 5 introduces the matching `sync` RPC + route. No migration: both columns already exist on the initial-schema tables and default to `1`. `ApiPremadeSubscriptionSchema` gains two required `z.number()` fields; `listSubscriptionsRaw` extends its `user_premade_subscriptions` SELECT to include `last_seen_version` and its `premade_decks` SELECT to include `version`. New integration test in `apps/api/tests/integration/premade.routes.test.ts` creates a test-scoped premade deck (isolated from the seed catalogue), subscribes, asserts the equality invariant on fresh subscriptions, then bumps the source version to 2 and re-GETs to verify `version > lastSeenVersion` end-to-end.

> **Superseded 2026-05-17 — model change to copy-as-starting-point.** The subscribe-with-version model has been replaced by the copy model. See the paragraph below for the shipped Stage 4 rewrite.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 12 — Sentence-layout schema contract + CHECK**: closes the open-shape gap on sentence-layout cards. `SentenceFieldsDataSchema` in `packages/shared-types/src/schemas/field-shapes.schema.ts` swaps from `z.record(z.string(), z.unknown())` to a concrete `{ ja, en, furigana, breakdown?, audio?, nuance? }` shape; migration `supabase/migrations/20260612000000_sentence_layout_check.sql` tightens the `cards_fields_data_shape` CHECK to require `ja`/`en`/`furigana` for `layout_type = 'sentence'` (vocabulary/grammar arm unchanged). Uses `NOT VALID` + `VALIDATE CONSTRAINT` per docs/DATABASE.md; a `DO`-block guard verifies no existing rows would violate before the swap. `getSentenceFrontBack` helper updated to read the new `ja`/`en` fields. Two type sites needed unknown-casts where the permissive wire `Record<string, unknown>` no longer matches the tight `FieldsData` union (cards.controller + generated-review-client preview builder). Leech-service drill-session row schemas now use the shared `FieldsDataSchema` directly. Re-exports for the field-shapes schemas added to shared-types/index.ts. Stage 8 + leech-service unit-test fixtures updated to the canonical shape. Coverage: 9 unit + 3 integration tests. Stage 11 (personal-best persistence) was skipped per user direction; references removed from kanban + this file.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 10 — Confusable-items detection**: new `GET /api/v1/insights/confusable-pairs?limit=…` returns the user's top card pairs they've mis-rated in the same review session AND that are semantically similar (cosine ≥ 0.70). Migration `supabase/migrations/20260611000000_confusable_pairs.sql` adds the `confusable_pairs` table with PK `(user_id, card_a, card_b)` + `CHECK (card_a < card_b)` (idempotent at the schema layer — canonical ordering means every unordered pair has exactly one row), the `record_confusable_pairs()` SECURITY DEFINER detection function (single-statement CTE pipeline processing all users; cards without embeddings excluded), the `get_confusable_pairs(p_user_id, p_limit)` reader RPC, and a defensive cron schedule wrapped in a `DO`-block so the migration applies cleanly without pg_cron. Wire shape `ApiConfusablePairSchema` with nested `cardA`/`cardB` display fields. Service `listConfusablePairs` + controller + route on the existing `/api/v1/insights` mount. Coverage: 5 unit + 8 integration tests including idempotency and threshold negatives.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 9 — Per-day maturity snapshots**: new `GET /api/v1/insights/maturity-history?days=90|180|365` returns per-state card counts per day for the requested window. Migration `supabase/migrations/20260610000000_card_state_snapshots.sql` adds the `card_state_snapshots` table (PK `(user_id, snapshot_date)`, RLS), the `record_card_state_snapshots()` SECURITY DEFINER cron function (one INSERT per profile, FILTER aggregates per FSRS state, `ON CONFLICT DO UPDATE`), and the `get_maturity_pipeline_history(p_user_id, p_days)` RPC which reads historical rows from the snapshot table and computes today live so the chart reflects the current moment between cron runs. Defensive `cron.schedule` wrapped in a `DO` block — applies cleanly on projects without pg_cron enabled (the application works without the cron; only the historical archive stops growing). Wire types: `ApiMaturitySnapshotSchema` + `ApiMaturityHistoryDaysSchema`. Service `apps/api/src/services/insights.service.ts::listMaturityHistory` parses the string days enum and maps the RPC's SQLSTATE 22023 to 400. Coverage: 6 unit + 7 integration tests.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 8 — Card-quality issue counts**: new `GET /api/v1/insights/card-quality` returns six rows of `{ issueType, count }` derived from `fields_data` JSONB key presence on the user's vocabulary+grammar cards (`missing_reading`, `missing_meaning`, `missing_example`, `missing_mnemonic`, `missing_picture`, `missing_nuance`). Migration `supabase/migrations/20260609000000_get_card_quality_issues_rpc.sql` ships `get_card_quality_issues(p_user_id)` as a single-scan FILTER aggregate + LATERAL VALUES unpivot — the six rows are always emitted, even when zero, so the consumer iterates a stable shape. Sentence-layout cards excluded server-side. Service `apps/api/src/services/insights.service.ts::listCardQualityIssues` + controller `cardQuality` + new GET route on the existing `/api/v1/insights` mount. Wire types: `ApiCardQualityIssueSchema` + `ApiCardQualityIssueTypeSchema`. The frontend `cards-quality-bars.tsx` enum mismatch (kebab-case vs snake_case, plus `missing-audio` / `missing-kanji-breakdown` orphans) is a known follow-up. Coverage: 5 unit + 6 integration tests.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 7 — Problem-card list (lapse-bucketed)**: new `GET /api/v1/insights/problem-cards?bucket=…` returns the user's cards in one of `'2-3' | '4-5' | '6-7' | '8plus'`. Backed by SECURITY DEFINER RPC `get_problem_cards(p_user_id, p_bucket)` in `supabase/migrations/20260608000000_get_problem_cards_rpc.sql` — excludes suspended cards, ordered by `last_review DESC NULLS LAST, id DESC`. The 8plus bucket cardinality equals the unresolved-leech count for the same user (`process_review` inserts a leech at lapses >= LEECH_THRESHOLD). New service `apps/api/src/services/insights.service.ts` + matching controller, route, and `.strict()` query schema mounted at `/api/v1/insights`. Wire types: `ApiProblemCardSchema` + `ApiProblemCardBucketSchema` in `packages/shared-types/src/schemas/api.schema.ts`. The `/insights/mistakes` page that originally consumed this endpoint was retired in the 2026-05-17 IA restructure — the endpoint ships consumer-agnostic for future surfaces. Coverage: 5 unit tests + 6 integration tests.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 6 — Tomo daily note API**: new `GET /api/v1/tomo/note` returns `{ body, kind: 'insight' | 'idiom', dateKey }`. The AI path lives in `apps/api/src/services/ai.service.ts::generateTomoNote` with `TOMO_NOTE_PROMPT_VERSION = 'v1'` baked into a Redis cache key (`tomo:note:v1:{userId}:{dateKey}`, 36h TTL) so OpenAI cost is bounded to one call per learner per day. The orchestration service `apps/api/src/services/tomo-note.service.ts` resolves the learner's timezone, derives `dateKey` + `yesterdayDateKey`, fetches profile + interests + yesterday's heatmap row in parallel, calls the generator, and catches every AI failure mode to substitute a deterministically-picked idiom from `apps/api/src/data/idioms.json` (curated; indexed by JLPT level; 3–4 entries per level). Route is mounted at `/api/v1/tomo` behind auth + default + AI-minute + AI-daily rate limiters. Frontend wires `useTomoNote()` through `apps/web/lib/api/tomo.ts`; `liveTomoNote` in `review-staging-client.tsx:236` is now sourced from the query result. Coverage: unit tests for the pure helpers + cache-key version isolation in `ai.service.test.ts` + integration test for the wire shape and same-day cache stability. No DB migration.

Re-touched 2026-05-17 for the **Backend Completion Plan Stage 4 rewrite — Premade "copy to library" migration**: replaces the subscribe-with-sync model with copy-as-starting-point. Migration `supabase/migrations/20260607000000_premade_copy_model.sql` drops `user_premade_subscriptions`, drops `decks.is_premade_fork`, drops `premade_decks.version` + its CHECK, drops `subscribe_to_premade_deck` / `unsubscribe_from_premade_deck`, and adds `copy_premade_deck(p_user_id, p_premade_deck_id)` returning `(deck_id, card_count)`. Route `POST /api/v1/premade-decks/:id/subscribe` → `POST /:id/copy`; `GET /api/v1/premade-decks/subscriptions/me` and `DELETE /:id/subscribe` are removed. The new RPC is SECURITY DEFINER + pinned search_path + GRANT EXECUTE to service_role; bulk-clones source cards with fresh FSRS state (`state=0`, `due=NOW()`, `reps=0`, `lapses=0`) and carries `embedding` + `embedding_updated_at`; allows duplicates by design (no uniqueness check on `(user_id, source_premade_id)`). Shared types: `ApiPremadeSubscriptionSchema` + `ApiSubscribeResultSchema` removed; `ApiCopyPremadeDeckResultSchema` added; `isPremadeFork` dropped from `ApiDeckSchema`; `version` dropped from `ApiPremadeDeckSchema`. `deck.service.ts::deleteDeck` simplified — no special path for premade forks. Coverage: rewritten unit suite for `copyPremadeDeck` (success / 404 / empty RPC / duplicate / generic dbError) and rewritten integration suite covering the four plan acceptance criteria (fresh copy, duplicate copy, copy-after-delete, inactive deck → 404).

Refreshed by code inspection on 2026-05-14 (Stage 8 + docs cleanup: removed duplicate "AI leech diagnosis" Partial row already covered by the Stage 7 Implemented row, and the "Dashboard weekly summary API" Missing row — superseded by heatmap-derived seven-day activity, no surface needs the endpoint).

Re-touched 2026-05-14 to record the IA import: new canonical IA lives under [information_architecture/](information_architecture/README.md). The current frontend lags the IA on route names, missing surfaces (review setup, problem-card repair, add-flow, library hierarchy, insights split), Settings sections (Display, Data & sync, Review behavior), and unresolved terminology (Leech vs Problem Card, Listening modality coverage). Active reconciliation tasks tracked in [KANBAN_BOARD.md](KANBAN_BOARD.md) under the "IA wireframe doc cleanup", "Reconcile IA card types", "Resolve Problem Card vs Leech", and "Settings IA: missing sections" cards.

Re-touched 2026-05-15 for the **Today client readability pass**: pure helpers from `apps/web/app/(app)/today/_components/today-client.tsx` extracted into `today-due-queue.ts` (queue math) and `today-preview-data.ts` (dev preview mocks); greeting helpers moved to shared `apps/web/lib/japanese-greeting.ts` (deduplicates the same logic from the login page). Banner-comment readability passes applied to `today-hero.tsx`, `section-primitives.tsx`, and `week-rhythm-strip.tsx`. No functional or visual changes — verified via `bun run --filter @fsrs-japanese/web typecheck`, `lint`, and `build`.

Re-touched 2026-05-14 for the **App Router → IA rename** (phase 1 of the migration): `/dashboard`→`/today`, `/analytics`→`/insights`, `/decks/browse` removed, `/profile` folded into `/settings`, card detail hoisted to `/cards/[cardId]`, review staging moved under `/review/setup`; stubs scaffolded for `/add`, `/add/review`, `/cards`, `/cards/[cardId]/repair`, `/decks/[id]/preview`, `/insights/{mistakes,progress,forecast,statistics}`. Stubs render a page title + outgoing IA links only — implementation work is tracked per-surface in [status/FRONTEND.md](status/FRONTEND.md).

This records current implementation evidence, not product intent. Product truth lives in [PRODUCT.md](PRODUCT.md), design truth in [DESIGN.md](DESIGN.md), database truth in [DATABASE.md](DATABASE.md), and active tasks in [KANBAN_BOARD.md](KANBAN_BOARD.md).

`bun run typecheck` was executed for this refresh. No runtime flows were exercised.

## Status Legend

- `Implemented`: code paths exist for the core behavior.
- `Partial`: meaningful code exists, but the product requirement is incomplete, placeholder-backed, or missing a major workflow.
- `Missing`: no active implementation path was found.
- `Unknown`: not enough evidence from static inspection.
- `Manual`: documented process or manual validation only; no automated checker was found.

## Current Status Files

- [Backend and Data](status/BACKEND.md)
- [AI and Japanese Content](status/AI_AND_JAPANESE.md)
- [Frontend](status/FRONTEND.md)
- [Testing](status/TESTING.md)

## Current Summary

| Area | Implemented | Partial | Missing | Unknown | Manual |
|---|---:|---:|---:|---:|---:|
| Backend and Data | 22 | 2 | 1 | 0 | 0 |
| AI and Japanese Content | 6 | 2 | 2 | 0 | 0 |
| Frontend | 10 | 10 | 2 | 0 | 0 |
| Testing | 3 | 0 | 1 | 0 | 1 |

## Archive

Release snapshots belong in [status/archive/](status/archive/). When a release ships, copy the current status index and area files into that archive with the release label before refreshing status for the next active scope.
