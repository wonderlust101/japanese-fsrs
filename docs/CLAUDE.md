# CLAUDE.md (docs/)

> **Canonical AI-assistant instructions live in [`/CLAUDE.md`](../CLAUDE.md) at the repo root.**
>
> This stub file exists so tooling that scans `docs/` for a `CLAUDE.md` keeps finding one. It intentionally contains no operational instructions, to prevent drift between two copies (a previous version of this file disagreed with the root on the SRS library — `@open-spaced-repetition/binding` vs. `ts-fsrs` — when the code uses `ts-fsrs`).
>
> All tech-stack, architecture, FSRS state-machine, common-pitfall, and convention references should be read from `/CLAUDE.md` and the linked design docs (`PRD.md`, `TDD.md`, `IA-WIREFRAME.md`, `UX-UI-SPEC.md`, `CONVENTIONS.md`) in this directory.
