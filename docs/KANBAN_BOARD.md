---

kanban-plugin: board

---

Source for status: [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md), refreshed 2026-05-19 (TODO-sweep audit pass — added cards-browser filter dims, post-save iteration UI, and `card-fields.ts` redundancy audit). Detail per area lives under [status/](status/); migration-level detail lives in each `supabase/migrations/*.sql` header.

## To Do

- [ ] **Cards browser — relax `missingField`/`presentField` mutual-exclusion (backend follow-up)**
  - Frontend popover allows users to express cross-direction combos like "has audio AND missing pitch". The v1 backend guard (`.refine` + RPC `invalid_filter_combination`) rejects these. Frontend currently picks presentField over missingField when both are set; a small backend amendment to drop the cross-direction restriction (same-dimension contradictions remain impossible by widget design) would let the natural popover UX work end-to-end.
- [ ] **/add/review post-save iteration UI**
  - The post-save regen branch (cheap `/generate-sentences` + `/generate-mnemonic`) is already wired at `generated-review-client.tsx:268` but unreachable — `SuccessBlock` replaces the form on save. Needs a read-only seed + "tweak more" affordance that PATCHes back.
- [ ] **Audit `card-fields.ts` Lapis-field helper for redundancy**
  - `components/review/session/card-fields.ts:8-12` claims the API schema doesn't expose pitch/picture/nuance/audio fields, but Stages 1 + 2 (2026-05-17) admitted them on `WordFieldsSchema` / `ExampleSentenceSchema`. Verify; if true, delete the helper and consume the schema directly.
- [ ] **Dashboard backend contracts**
  - Replace remaining placeholder dashboard surfaces with explicit contracts. Deck rollup shipped as Stage 3.
- [ ] **Launch-size premade catalogue** — content work; expand JLPT + Joyo + grammar coverage beyond starter seeds.
- [ ] **Frontend test coverage** — pick a runner; cover review / onboarding / premade-browse / analytics flows.
- [ ] **Legal pages** — Privacy Policy + ToS.
- [ ] **App-level system pages** — add `loading.tsx` skeletons for `/today`, `/review/setup`, `/insights/*`. `error.tsx` + `not-found.tsx` already shipped.
- [ ] **Public brand landing page** — replace the `app/page.tsx` 7-line redirect with a designed brand surface.
- [ ] **Public SEO + installability** — add `app/manifest.ts` + maskable icons + JSON-LD; expand `sitemap.ts`.
- [ ] **Full frontend CSP nonce policy** — extend the current `frame-ancestors`-only CSP once Next nonce handling is designed.

## In Progress

- [ ] **Frontend polish pass (very fine finishes)** — single rolling item for the remaining UI refinement work; tackle in any order, mark sub-items as they land.
  - **Design drift fixes on touch** — no broad sweep; fix whatever's visibly wrong on the surface you happen to be in.
  - **Home / decks / masthead** — deck-list row styles, My Decks layout, masthead background, analytics nav icon.
  - **Remove visible AI wording** — outcome copy (create / suggest / draft) replaces `Generate with AI` labels; internal code/docs terms stay.
  - **Japanese metadata surfaces** — wire pitch accent / frequency / collocations / kanji breakdown into the existing card-detail / review-back chrome (schemas already carry the fields).

## Review

- [ ] **Verify custom system-page coverage** — confirm broad `error.tsx` / `not-found.tsx` / `loading.tsx` coverage before promoting to Done.

## Done

- [x] **Cards browser — pitch/image/audio filter dimensions (2026-05-19)** — full backend + frontend slice. **Backend** (migration `20260624000000_cards_browser_presence_filters.sql`): `count_moras()` SQL helper, three partial JSONB expression indexes on `cards.fields_data` (`cards_has_picture_idx`, `cards_has_audio_idx`, `cards_has_pitch_idx`), `CREATE OR REPLACE` of `list_cards_cross_deck` + `count_cards_cross_deck` with `p_present_field` (`picture`/`pitch`/`audio`) and `p_pitch_pattern` (`heiban`/`atamadaka`/`nakadaka`/`odaka`); existing `p_missing_field` whitelist extended with `pitch` and `audio`. Shared schema gains `cardPresentFieldEnum` + `pitchPatternEnum`, and `crossDeckListCardsQuerySchema.refine` rejects payloads that set both directions. **Frontend**: new `more-filters-popover.tsx` (portal-rendered, staged Apply/Reset, outside-click + Escape close, focus moved into popover on open, prefers-reduced-motion respected) replaces the toast stub at `cards-browser-view.tsx:336`. Three independent dimensions — pitch presence + optional pattern, image presence, audio presence. URL deep-link via `?present=` and `?pitchPattern=` (read-once on mount, mirroring the existing `?missing=` pattern). Active-filter count badge on the trigger chip. `SavedViewRecipe` extended with optional `presentField` / `pitchPattern` (recipes are code-shipped so no localStorage migration needed). Coverage: 4 new service tests + 10 new shared-schema tests, all green. **Known v1 constraint** (tracked under To Do as a backend follow-up): the `.refine` cross-direction guard means when a user selects both directions in the popover, only the positive direction is sent to the API — relaxing the backend guard would unblock combinations like "has audio + missing pitch". **Display** of the new fields in the result-table rows stays under the rolling polish pass.
- [x] **Weak spots Phase 3 — drill follow-ups closed (2026-05-19)** — three small frontend deliverables, no backend changes. (1) **`hasWeakSpots` signal parity** — the count chip on the "Weak spots" nav row was plumbed at `nav-config.ts` but never reached the chrome surfaces because the dispatch to `WeakSpotCountNavItem` only fired in the parent-with-children code path inside `nav-item.tsx`, and the top-level Study section's items have no children. Fixed by exporting `WeakSpotCountNavItem` and dispatching on `item.hasWeakSpotCount` from both `sidebar.tsx` (`decorate`) and `mobile-drawer.tsx`, so the desktop + mobile chrome both show the count chip identically. Second nudge surface added at `/review/summary` as a calm one-liner with a `QuietLink` to `/insights/weak-spots/drill/setup` — distinct from the in-session `ProblemCardsCard` (which lists weak spots triggered *this session*); the new line surfaces the cumulative unresolved backlog and is DOM-absent during loading / when count is zero / in fixture mode. (2) **"Count this as a real review" override inside drill** — `drill-session-client.tsx` gains an inline 4-channel confirm panel that activates while `showAnswer` is true; the override link sits in a fixed-position strip above the existing 3-channel `DrillRatingBar` (keymap: `R` opens, `Esc` cancels, `1-4` map to Again/Hard/Good/Easy while the panel owns the keymap). The submit path calls the canonical `useSubmitReview` mutation, advances the local queue with `countedAsReview: true` (the `DrillAttemptRecord` flag was already in the store schema), skips the drill-attempt network write to avoid polluting `weak_spot_drill_attempts` with a marker the wire can't carry, and invalidates `queryKeys.weakSpots.all()` so the unresolved-count badge refreshes. (3) **Diagnosis "About this read" disclosure** — `weak-spot-diagnosis-panel.tsx` adds a closed-by-default `MoreDisclosure` under the prescription line, rendered only when `hasContent`; copy stays generic ("Tomo's read is generated from this card's recent lapse pattern and review history. It doesn't change your schedule.") so `DIAGNOSIS_PROMPT_VERSION` does not need to be exposed to the web layer. Verified via `bun run --filter @fsrs-japanese/web typecheck` / `lint` / `build` (all green).
- [x] **Settings redesign + MVP scope cut (2026-05-19)** — `/settings/*` migrated from the centered 52rem column to the canonical 1440px frame matching Insights / Cards / the rest of the (app) shell. Horizontal tab bar sticks beneath the `TopBar` at full canvas width with kanji-led tabs (人 Account · 学 Learning · 鍵 Security) and a 2px Inari Vermillion underline marking the active route; auto-scrolls the active tab into horizontal center on route change so the current section is never off-screen at narrow widths. New shared primitives under `apps/web/app/(app)/settings/_components/`: `SectionShell` (per-page wrapper that pairs the card with an optional right strip on `xl`), `ContextStrip` / `ContextNote` / `Kbd` (the right context column — used on every section). New field in Account: **study goal** (`textarea`, wired through `updateProfileAction({ studyGoal })`); the schema field has been live since the original profile schema landed but had no UI until now. **MVP cut:** the IA's Review behavior / Display / Data & sync sections were designed and prototyped, then deleted entirely rather than shipping as "Preview only" — none of their backend exists yet (apkg import/export, sync layer, offline asset cache, dark-mode palette, global theme/font/density plumbing) and Tomo's brand stance refuses fake UI. They will return when their backend lands. **Result:** three tabs, 13 controls, all 13 wired (`updateProfileAction` for 10 of them, `supabase.auth.updateUser` for email + display name, the three security actions for password / sign-out-all / delete). User menu's redundant "Profile" item also removed (the IA spec at `docs/information_architecture/18_settings.md:78` explicitly forbids duplicate Profile and Settings destinations — the menu was the only place that violation lived). **Closes:** "Settings IA: missing sections" To Do; "Settings V3 + Profile redesign" sub-bullet of the rolling polish pass.
- [x] **Premade "copy to library" — frontend slice complete (2026-05-19)** — Attribution chip on `DeckCard` (`sourcePremadeName` prop → quiet "· From: <name>" after cards-count); `deck-list.tsx` resolves the name via `usePremadeDecks()` map, deduped with `/decks/premade` via TanStack queryKey. `DeleteDeckDialog` copy unified with the bulk dialog's wording + typographic apostrophe.
- [x] **Deck archive + duplication: frontend wiring + remaining backend gates (2026-05-19)** — **Frontend duplication:** `lib/api/decks.ts` gains `useCopyDeck`/`useArchiveDeck`/`useUnarchiveDeck`; matching actions in `lib/actions/decks.actions.ts`; `listDecksAction` accepts `view: 'active'|'archived'|'all'`. `deck-list.tsx` `handleCopy`/`handleBulkCopy` fire the real mutation (empty body → server default name); bulk copy uses `Promise.allSettled`. **Frontend archive:** `useArchiveSet` derives from `useDecks(50, 'all')` instead of localStorage; one-shot LS migration POSTs legacy `tomo.decks.archived.v1` entries to `/archive` then sets a `…v1.migrated` sentinel. **Backend gates:** new `assertCardDeckActive(cardId, userId)` in `deck.service.ts` wired into `cards.controller.ts` (`update`/`forget`/`reschedule`/`suspend`/`unsuspend`/`regenerateEmbedding`/`move`/`copy`), `ai.controller.ts` (`generateSentences`/`generateMnemonic`, pre-OpenAI), and `weak-spots.controller.ts createDrillSession` (hard-fail on `deckScoped`/`currentCard` targets; other sources skip silently inside RPC). Migration `20260623000000_archive_aware_forecast_and_drill.sql` rewrites `get_review_forecast` + `create_weak_spot_drill_session` with the archived-decks exclusion CTE. **Deferred:** bulk-card-mutation endpoints (`/bulk/move`, etc.) — separate task.
- [x] **FSRS optimizer surface removed (2026-05-18)** — Deletion, not deferral. Stripped `OptimizationStatus.tsx`, the "Optimization status" card in `FsrsSection.tsx`, the `optimizationStatus`/`lastOptimizedAt` fields on `FsrsState`, and the `'no-fsrs'` dev fixture key. FSRS section now renders four visualizations (dial, retention comparison, stability/difficulty histograms). Reason: no refit process planned; `'never-run'`-forever was worse than no surface. Per-deck scheduling controls in `deck-options-panel.tsx` also closed; duration is wired via `review_logs.review_time_ms`, never a heuristic.
- [x] **IA stub pages retired (2026-05-19)** — `/decks/[id]/preview` shipped 2026-05-18 (read-only viewer). `/cards/[cardId]/repair` shipped earlier (commit `346813c`) — live `repair-view.tsx` backed by `useForgetCardMutation`/`useRescheduleCardMutation` + `POST /cards/:id/forget` and `/reschedule`, linked from `card-detail-view.tsx` (`CardActionsStrip.repairHref` + inline "Repair recommended" hint when `lapses > 0`). `grep -rn 'StubPage'` returns zero matches.
- [x] **/add/review regen handlers wired to dedicated AI endpoints (2026-05-19)** — `saveCardAction` returns the inserted `ApiCard` (validated against `ApiCardSchema`). `generated-review-client.tsx` captures `savedCardId` and branches `onRegenSentence`/`onRegenMnemonic`: pre-save → `generateCardPreviewAction`; post-save → cheap dedicated `/ai/generate-sentences` + `/generate-mnemonic` (~80% smaller OpenAI payload). **Post-save branch is wired but not user-reachable** — `SuccessBlock` replaces the form on save; activates once the post-save iteration UI lands (tracked under that To Do entry). No backend changes.
- [x] **Deck archive backend (2026-05-19)** — `decks.archived_at TIMESTAMPTZ NULL` persists state across devices. Routes `POST /decks/:id/archive` + `/unarchive` (idempotent; preserves original timestamp on re-archive); `GET /decks` accepts `view=active|archived|all`. `ApiDeckSchema` adds `archivedAt`. Archive is a freeze: `assertDeckActive` (404 `DECK_NOT_FOUND` / 422 `DECK_ARCHIVED`) gates `PATCH /decks/:id`, `/copy`, `/cards`. Reviews blocked at FSRS service layer (single throws 422; batch surfaces per-row). `get_due_cards` excludes via shared CTE. Migration `20260622000000_deck_archive.sql` carries the column, partial index `decks_user_id_active_idx WHERE archived_at IS NULL`, and `list_decks_paginated`/`get_due_cards` redefinitions. 18 unit tests. **Follow-ups closed in the 2026-05-19 frontend+gates PR.**
- [x] **Deck duplication backend (2026-05-18)** — `POST /api/v1/decks/:id/copy` ships. Body `{ name?: string }` (`.strict()`); empty body defers to the server's `<source> (Copy [N])` default-name loop. Returns 201 `{ deckId, cardCount }` + `Location` header. New migration `supabase/migrations/20260621000000_copy_user_deck_rpc.sql` adds the `copy_user_deck` SECURITY DEFINER RPC (atomic deck row + card clones). Cloned cards reset FSRS state, drop tags / `is_suspended` / `parent_card_id`, carry embeddings; suspended source cards are skipped. Premade source decks and cross-user attempts both 404 `DECK_NOT_FOUND` (no ownership-leak). Idempotency-keyed via `withIdempotency`; reuses `subscribeRateLimitMiddleware` for the per-user lockout (same blast radius as premade copy). 9 unit tests in `apps/api/src/services/__tests__/deck.service.test.ts`. **Frontend follow-up:** wire `apps/web/app/(app)/decks/_components/deck-list.tsx:357,368` (`handleCopy` / `handleBulkCopy`) to call the new endpoint; the "Copy is coming soon" toast is still in place.
- [x] **Insights Progress + Statistics — final wire-level signals (2026-05-18)** — Progress's `desiredRetention` now reads `profile.retentionTarget` (Statistics already did) and `cardsAddedThisMonth` flows through the analytics dashboard's new tz-aware field instead of a hardcoded zero. Statistics's four previously empty histograms (rating distribution, interval distribution, FSRS stability, FSRS difficulty) now consume `GET /api/v1/insights/distributions` — one bundled round-trip backed by four new SECURITY DEFINER RPCs in `supabase/migrations/20260618000000_*.sql`. The activity-strip "total time" figure dropped its `SECONDS_PER_REVIEW = 18` heuristic in favour of real `review_logs.review_time_ms` aggregation, exposed via the extended `get_heatmap_data` RPC (now returns `total_seconds` per day). The same migration also consolidates the accidental dual overload of `get_dashboard_data` (UUID) vs (UUID, TEXT) that the 20260529/20260604/20260614 chain left behind. FSRS `optimizationStatus` stays as `'never-run'` by intentional deferral (see To Do).
- [x] **Onboarding decks: copy-on-continue wired (2026-05-18)** — `/onboarding/decks` "Add and begin" no longer discards the learner's selections. `apps/web/app/onboarding/decks/page.tsx` now fires `updateProfileAction(...)` and one `copyPremadeDeckAction(deckId)` per checked deck in parallel via `Promise.allSettled` (no new backend — the contract has been live since Stage 4 and is the same mutation the `/decks/premade` catalogue already uses through `useCopyPremadeDeck`). Profile-mutation failure surfaces inline and blocks navigation; partial deck-copy failures still navigate but show a summary `Toast` so the learner can finish the job from `/decks/premade`. Continue button gates on both phases via `continueLoading={isBusy}`. Skip path unchanged. Remaining onboarding work is the interest-based scoring follow-up (open in To Do).
- [x] **Today practice-signal card (Phase 3, 2026-05-18)** — IA `01_today.md`'s "concise pre-session note (weak-point reminder)" slot now lives between Hero and WeekRhythmStrip. New `apps/web/app/(app)/today/_components/today-weak-spots-card.tsx` consumes `useWeakSpotsQuery({ status: 'unresolved', limit: 3, sort: 'mostRecent' })` (same cache the sidebar count badge already populates — no new backend), renders a count + up to 2 compact preview rows (word/reading via `FuriganaText`, deck name, lapse count, JLPT pill — no diagnosis line, no per-row actions), and surfaces two `QuietLink` CTAs (`Drill these weak spots →` to `/insights/weak-spots/drill/setup`, `See all →` to `/insights/weak-spots` only when more than the preview limit are outstanding). Calm-first: silent during `isLoading`/`isError`, DOM-absent when the learner has no unresolved weak spots (no card, no skeleton, no empty state). The other Phase 3 follow-ups (`hasLeeches` signal, drill "Count this as a review" override) remain open under the To Do entry.
- [x] **/cards browser — full live wire-up (2026-05-18)** — `/cards` off dev fixtures. New endpoints: `GET /cards/cross-deck` (search + deck/jlpt/status/missing-field + recent|due|lapses sort + cursor), `POST /:id/{move,copy,suspend,unsuspend}` and `bulk/{move,suspend,unsuspend,delete,tag}` (idempotency-keyed; bulk returns per-id `{succeeded, failed}`). Migrations `20260617000000_list_cards_cross_deck_rpc.sql` + `20260617000001_card_move_copy_suspend_rpcs.sql` carry nine new SECURITY DEFINER RPCs. Frontend: `lib/api/cards.ts` hooks, `cards-browser-view.tsx` rewritten, row actions live, `CardsBulkBar` mounted on selection, `CardsQualityBars` reads `/insights/card-quality`, "more filters" toggle surfaces a stub notice for pitch/image/audio (tracked separately under To Do). Saved views shipped as curated presets in `localStorage`; quality bars deep-link via `?missing=<field>`.
- [x] **Sweep stale subscription language (2026-05-18)** — verified across `TDD.md`, `IMPLEMENTATION_STATUS.md`, `status/*`, and the IA wireframes. Every surviving mention of "subscribe / subscription / lastSeenVersion" is one of three legitimate roles: (a) historical record of what the Stage 4 migration removed (`status/BACKEND.md`), (b) copy-model contrast framing that defines the new model by what it *isn't* (TDD.md, sitemap, deck-preview header), or (c) explicit anti-pattern guidance for designers (`12_deck_preview.md:134` "do not imply ongoing relationship"). No surface promotes the old vocabulary.
- [x] **IA wireframe doc cleanup (2026-05-18)** — stray absolute path stripped from `00_sitemap.md`, "Lapis-compatible note" rewritten as "Tomo card note", and the "legacy design system" framing dropped from `03_review_session.md`. Low-risk hygiene pass against the IA wireframes.
- [x] **Collapse `card_type` modality → single FSRS scheduler (2026-05-18)** — migration `20260614000000_drop_card_type.sql` drops `cards.card_type`, the `card_type` enum, and the per-modality scheduler map in `fsrs.service.ts`; one scheduler at `request_retention = 0.85` (matches `profiles.retention_target` default). Bonus: `get_accuracy_by_layout` renamed to `get_accuracy_by_layout_type` and re-pointed at `c.layout_type` (the old name was a misnomer that grouped by `card_type` while exposing the column as `layout`). Closes "Reconcile IA card types with `card_type` schema" — the IA's modality framing in `03_review_session.md` is also restructured around `layout_type` in the same PR.
- [x] **Rename leech → weak spot end-to-end (2026-05-18)** — migration `20260615000000_rename_leeches_to_weak_spots.sql` renames the four DB tables, twelve indexes, and drops + recreates the four drill RPCs (`create_weak_spot_drill_session`, `get_weak_spot_drill_session`, `record_weak_spot_drill_attempt`, `transition_weak_spot_drill_session`). Also recreates `process_review`, `process_review_batch`, `get_session_summary` with renamed table refs because plpgsql function bodies are stored as text and don't re-parse on table rename. Wire-level: `/api/v1/leeches/*` → `/api/v1/weak-spots/*`; `LEECH_ALREADY_OPEN` → `WEAK_SPOT_ALREADY_OPEN`; env var `LEECH_THRESHOLD` → `WEAK_SPOT_THRESHOLD`; wire field `leechId` → `weakSpotId`. Backend files moved: `leech.service.ts`/`leeches.controller.ts`/`leeches.ts` (routes) → `weak-spot.*`/`weak-spots.*`. Frontend: every `Leech*` / `useLeech*` / `Leeches*` export, hook, store, query-key family, Pill tone, and IA wireframe renamed; `ProblemCardRow` → `WeakSpotRow`; `Problem cards` → `Weak spots` everywhere. IA `05_problem_card_repair.md` → `05_weak_spot_repair.md`. Closes "Resolve 'Problem Card' vs 'Weak spot' vocabulary". Follow-ups: regenerate `database.types.ts` via `bun gen:types` to replace the hand-edited copy; the DB CHECK source-value `unresolved_leeches` (internal enum, no learner impact) kept as-is.
- [x] **Sentence-layout AI generator branch (Stage 13, 2026-05-17)** — `mode='ai' layoutType='sentence'` now dispatches to `generateSentenceCard` (separate Redis namespace `sentence-card:v1:…`). Coverage: 6 unit + 2 cache-seeded integration tests. No migration — Stage 12 already shipped the CHECK.
- [x] **Sentence-layout schema contract + CHECK (Stage 12, 2026-05-17)** — closes the open-shape gap. `SentenceFieldsDataSchema` tightened to `{ ja, en, furigana, breakdown?, audio?, nuance? }`; migration `20260612000000_sentence_layout_check.sql` adds the matching DB CHECK via `NOT VALID` + `VALIDATE`. Stage 11 (personal-best persistence) was skipped per user direction.
- [x] **Confusable-items detection (Stage 10, 2026-05-17, removed 2026-05-18)** — Endpoint, RPC, table, detection cron, schema, and consumer types all removed. The feature shipped without a frontend consumer and a follow-up audit decided to retire it rather than build the UI. Application code, tests, shared-types entries, and IA-doc references stripped; a forward migration drops `confusable_pairs`, `record_confusable_pairs()`, `get_confusable_pairs()`, and the cron schedule. Original migration `20260611000000_confusable_pairs.sql` left in place as historical record.
- [x] **Per-day maturity snapshots (Stage 9, 2026-05-17)** — `GET /api/v1/insights/maturity-history?days=90|180|365`. Migration `20260610000000_card_state_snapshots.sql` adds the snapshot table, `record_card_state_snapshots()` cron job (02:15 UTC), and `get_maturity_pipeline_history()` which always computes today live. Mature = state 2 + scheduled_days ≥ 21 (Anki convention; matches Stage 3).
- [x] **Card-quality issue counts (Stage 8, 2026-05-17)** — `GET /api/v1/insights/card-quality` returns six rows for `missing_{reading,meaning,example,mnemonic,picture,nuance}`. Migration `20260609000000_get_card_quality_issues_rpc.sql`; single-scan `COUNT(*) FILTER (…)` + `LATERAL VALUES` unpivot. Frontend `cards-quality-bars.tsx` consumes the same six-value enum end-to-end (verified 2026-05-18).
- [x] **Problem-card list (lapse-bucketed) (Stage 7, 2026-05-17, removed 2026-05-18)** — Endpoint, RPC, schema, and consumer types removed for the same reason as confusable-pairs: shipped without a consumer, retired after a follow-up audit. A forward migration drops `get_problem_cards()`; the original migration `20260608000000_get_problem_cards_rpc.sql` stays as historical record.
- [x] **Tomo daily note API (Stage 6, 2026-05-17)** — `GET /api/v1/tomo/note` returns `{ body, kind, dateKey }`. AI path via `generateTomoNote` (`TOMO_NOTE_PROMPT_VERSION='v1'`, 36h Redis TTL); deterministic idiom fallback from `data/idioms.json` keyed by SHA-256 of `(userId, dateKey)` whenever AI fails. No DB migration.
- [x] **Premade "copy to library" migration (Stage 4 — rewritten 2026-05-17, copy model)** — replaces the subscription/fork model with copy-as-starting-point. Migration `20260607000000_premade_copy_model.sql` drops `user_premade_subscriptions`, `subscribe_to_premade_deck`, `unsubscribe_from_premade_deck`, `premade_decks.version`, and `decks.is_premade_fork`; adds `copy_premade_deck(p_user_id, p_premade_deck_id)`. Route: `POST /api/v1/premade-decks/:id/copy` (allows duplicates by design). Frontend rebranding tracked separately.
- [x] **Surface premade `version` + `lastSeenVersion` (Stage 4, 2026-05-17)** — *superseded by the 2026-05-17 copy-model rewrite; the schema fields and route were removed by `20260607000000_premade_copy_model.sql`. Kept as historical record.*
- [x] **Dashboard deck rollup batch (Stage 3, 2026-05-17)** — `GET /api/v1/decks` returns per-deck due/new/mature/due-new/due-review/last-reviewed in one round-trip. Migration `20260606000000_list_decks_paginated_rollups.sql`. Frontend N+1 elimination is opt-in follow-up.
- [x] **AI generator population for Lapis-style fields (Stage 2, 2026-05-17)** — `generateCard` prompt now produces `pitchPosition` + `nuance`; explicitly omits `picture` / `expressionAudio` / `sentenceAudio` (assets the system can't host yet). `CARD_PROMPT_VERSION='v2'` invalidates the old cache.
- [x] **Card content fields for Lapis-style review UI — schema admission (Stage 1, 2026-05-17)** — additive `.nullable().optional()` fields on `WordFieldsSchema` + `ExampleSentenceSchema`: `picture`, `expressionAudio`, `pitchPosition` (int ≥ 0), `nuance`, `sentenceAudio`. No migration — `cards_fields_data_shape` only enforces required keys.
- [x] **Design-system migration — `lucide-react` retired (2026-05-17)** — zero usages remain across `apps/web/{app,components,lib}`; dependency removed from `apps/web/package.json` (verified 2026-05-18).
- [x] **Rename Leeches → Weak spots in user-facing surfaces (2026-05-17)** — route `/insights/leeches/*` → `/insights/weak-spots/*` (via `git mv`); all 25 URL references rewritten; kanji ornament 蛭 → 弱. Internal code (components, hooks, store, query keys) kept `leech*` at the time to stay aligned with `/api/v1/leeches`. *Superseded 2026-05-18 by the full leech → weak spot rename above — all internal identifiers now also use `weakSpot*`.*
- [x] **Retire /insights/mistakes (2026-05-17)** — page deleted after the IA restructure. Card quality bars moved to `/cards` behind a `欠 Card quality` toggle; pattern summary bars removed as redundant; weak spots mini-list canonicalized at `/insights/weak-spots`.
- [x] **Weak spots Phase 2 — Drill flow (2026-05-17)** — full drill experience under `/insights/weak-spots/drill/{setup,[sessionId],[sessionId]/summary}`. Bespoke 3-channel `DrillRatingBar` (Missed/Hesitated/Remembered); `useWeakSpotDrillSessionStore` (Zustand, renamed from `useLeechDrillSessionStore` in the 2026-05-18 sweep); scheduler-invariance honored. Phase 3 deferred: dashboard surface + "Count this as a review" override.
- [x] **Weak spots sidebar entry + unresolved-count badge (2026-05-17)** — `useUnresolvedWeakSpotCount()` projection over `useWeakSpotsQuery` (both renamed from `useUnresolvedLeechCount` / `useLeechesQuery` in the 2026-05-18 sweep); vermillion-stroked count chip capped at 50. Other nav rows pay zero subscription cost.
- [x] **Weak spots Phase 1 — List + Detail + Diagnosis (2026-05-17)** — `/insights/leeches` (renamed to `/insights/weak-spots` on the same day, internal identifiers renamed on 2026-05-18) lists unresolved/resolved weak spots with six filter dimensions; per-row Drill / View / Resolve actions; detail `Dialog` includes `WeakSpotDiagnosisPanel` calling `POST /api/v1/weak-spots/:id/diagnose` with fresh `Idempotency-Key`.
- [x] **Add Japanese two-path capture pass (2026-05-15)** — `/add` shows only Word/Sentence/Deck plus "Add manually" + "Generate card" buttons; all back-of-card capture deferred to `/add/review`. `useCaptureDraftStore.mode` carries `'generate' | 'manual'`.
- [x] **Add Japanese back-of-card-aware pass (2026-05-15)** — optional capture surface reframed around the actual back-of-card fields (Reading / Meaning / Mnemonic / Picture) plus Note / Source. **IA deviation:** Deck is required at capture.
- [x] **Add Japanese session-faithful preview pass (2026-05-15)** — symmetric 6/6 layout; `AddSessionPreview` renders the actual `CardFront` from the in-progress draft via a single `buildPreviewCard` helper.
- [x] **Add Japanese capture canonical-composition pass (2026-05-15)** — `/add` restructured to match Tomo's canonical page rhythm. Extracts shared primitives `PageHeader`, `MobileStickyActionBar`, `ExitLinksRow` used by Today / Review Setup / Settings too.
- [x] **Review Summary redesign (2026-05-15)** — closure-first page composed of `SectionCard`s with a six-pattern state classifier (strong / mixed / difficult / weak spot / ended-early / no-pattern). Session "End session" routes to Summary for symmetric calm close.
- [x] **Today client readability pass (2026-05-15)** — `today-client.tsx` 804 → ~365 lines via extracted pure helpers and shared `lib/japanese-greeting.ts`. No functional change.
- [x] **Canonical documentation cleanup** — product, design, database truth pinned to `docs/PRODUCT.md`, `docs/DESIGN.md`, `docs/DATABASE.md`.
- [x] **Auth and account management** — signup, OTP, login, refresh, logout, password change, account deletion.
- [x] **Profile preferences and interests** — profile CRUD, study prefs, JLPT target, daily limits, timezone, interests.
- [x] **Deck and card API** — CRUD, AI + manual creation, semantic similarity, embedding regeneration, idempotency, optimistic concurrency.
- [x] **Premade deck backend** — browse, subscribe, unsubscribe, self-healing. *(Subscribe path superseded by copy model 2026-05-17.)*
- [x] **FSRS review engine** — due queue, submit, batch, forecast, session summary, rollback/forget/reschedule, weak spot flagging.
- [x] **AI generation endpoints** — cards, sentences, mnemonics with prompt sanitization, structured-output validation, response caching.
- [x] **Analytics backend + UI** — heatmap, accuracy, JLPT gap, milestones, review forecast, bundled dashboard. Streaks removed in Stage 8. The four granular endpoints retired 2026-05-18 — see "Retire granular analytics endpoints" below.
- [x] **Retire granular analytics endpoints (2026-05-18)** — `/api/v1/analytics/{heatmap,accuracy,jlpt-gap,milestones}` removed. Application code stripped: 4 routes, 4 controllers, 4 service functions, and the matching unit tests. Coverage moved to a new `getDashboardData` projection test that exercises the snake→camel reshape, `accuracyPct` / `progressPct` rounding, null `daily_pace` fallback, and the bundled envelope shape. The bundled `/dashboard` endpoint is unaffected. Underlying SQL RPCs stay in place as historical record per forward-only policy. Mirrors the problem-cards / confusable-pairs retirement.
- [x] **Core frontend app shell** — auth, onboarding, dashboard, decks, premade browse, review, summary, analytics, settings, profile.
- [x] **Offline review queue** — failed submissions queued locally and replayed through the batch endpoint.
- [x] **API + shared-schema test coverage** — API unit + integration tests, shared auth schema tests.
- [x] **Weak spots list + detail (Stage 1)** — `GET /api/v1/weak-spots` (filters, sorts, tuple cursor) + `GET /:id`; orphans preserved via LEFT/INNER join switching. Commit `81a0b35` (2026-05-14).
- [x] **Weak spots resolve + reopen (Stage 2)** — idempotent flips; partial-unique-index 23505 → HTTP 409 `WEAK_SPOT_ALREADY_OPEN`.
- [x] **Weak spots list spec-alignment patch (Stage 2.5)** — adds `deckOrder` sort + `diagnosis: available | missing` filter.
- [x] **Drill session creation + snapshot (Stage 3)** — `POST /api/v1/weak-spots/drill-sessions` with `Idempotency-Key`; two new tables + SECURITY DEFINER RPC; zero FSRS writes.
- [x] **Drill session resume + stale detection (Stage 4)** — `GET /:sessionId` returns queue + `isCanonicalStateStale` + `staleCards`; shared `compute_card_state_fingerprint_v1` helper with migration-time self-test.
- [x] **Drill attempts + scheduler-invariance suite (Stage 5)** — `POST /:sessionId/attempts`; anti-fraud composite FK; `eventId` as the DB-level idempotency contract; 200-iteration property suite proves no `cards`/`review_logs` writes from the drill path.
- [x] **Drill session lifecycle + source expansion (Stage 6)** — `/finish` + `/abort` (idempotent, terminal one-way); five source values via four-branch `UNION ALL`.
- [x] **Coding-standards fix-up on diagnose (Stage 7.1)** — `withIdempotency` + strict empty-body schema + parallelized fetches + versioned cache key on `/diagnose`.
- [x] **Free MVP completion + AI weak-spot diagnosis (Stage 7)** — paid/free tier model removed; `POST /:id/diagnose` ships free; writes only `weak_spots.diagnosis` + `weak_spots.prescription` (originally `leeches.diagnosis` + `leeches.prescription`; table renamed 2026-05-18).
- [x] **Remove legacy streaks + expose rollback/forget/reschedule (Stage 8)** — migration `20260604000000_remove_legacy_streaks.sql` drops `get_streak` and rebuilds `get_dashboard_data` without it. **BREAKING:** `ApiAnalyticsDashboardSchema.streak` removed end-to-end. Newly exposed: `POST /reviews/:reviewLogId/rollback`, `POST /cards/:id/forget`, `POST /cards/:id/reschedule`.

%% kanban:settings

```
{"kanban-plugin":"board","lane-width":800}
```

%%
